<%- include('partials/header') %>
<%- include('partials/nav') %>

<div class="container page-shell">
  <!-- Barre de recherche et filtres -->
  <div class="page-toolbar">
    <form method="get" action="/employeurs" class="search-filter-form">
      <input type="text" name="search" class="page-search-input" placeholder="Rechercher par nom, prénom, poste..." value="<%= filters?.search || '' %>" />
      <select name="departement" class="filter-select">
        <option value="">Tous les départements</option>
        <option value="Direction" <%= filters?.departement === 'Direction' ? 'selected' : '' %>>Direction</option>
        <option value="Finances" <%= filters?.departement === 'Finances' ? 'selected' : '' %>>Finances</option>
        <option value="RH" <%= filters?.departement === 'RH' ? 'selected' : '' %>>RH</option>
        <option value="Marketing" <%= filters?.departement === 'Marketing' ? 'selected' : '' %>>Marketing</option>
        <option value="Ventes" <%= filters?.departement === 'Ventes' ? 'selected' : '' %>>Ventes</option>
        <option value="IT" <%= filters?.departement === 'IT' ? 'selected' : '' %>>IT</option>
        <option value="Operations" <%= filters?.departement === 'Operations' ? 'selected' : '' %>>Operations</option>
        <option value="Service Client" <%= filters?.departement === 'Service Client' ? 'selected' : '' %>>Service Client</option>
        <option value="Autre" <%= filters?.departement === 'Autre' ? 'selected' : '' %>>Autre</option>
      </select>
      <select name="grade" class="filter-select">
        <option value="">Tous les grades</option>
        <option value="Employé" <%= filters?.grade === 'Employé' ? 'selected' : '' %>>Employé</option>
        <option value="Manager" <%= filters?.grade === 'Manager' ? 'selected' : '' %>>Manager</option>
        <option value="Responsable Sécurité" <%= filters?.grade === 'Responsable Sécurité' ? 'selected' : '' %>>Responsable Sécurité</option>
        <option value="Chef d'Équipe" <%= filters?.grade === "Chef d'Équipe" ? 'selected' : '' %>>Chef d'Équipe</option>
        <option value="Directeur" <%= filters?.grade === 'Directeur' ? 'selected' : '' %>>Directeur</option>
        <option value="Cadre Supérieur" <%= filters?.grade === 'Cadre Supérieur' ? 'selected' : '' %>>Cadre Supérieur</option>
        <option value="Autre" <%= filters?.grade === 'Autre' ? 'selected' : '' %>>Autre</option>
      </select>
      <button class="page-search-btn" type="submit">🔍</button>
      <% if (filters?.search || filters?.departement || filters?.grade) { %>
        <a href="/employeurs" class="btn-reset">✖ Réinitialiser</a>
      <% } %>
    </form>
  </div>

  <!-- En-tête avec statistiques -->
  <div class="page-header">
    <h1>🧑‍💼 Employeurs</h1>
    <p>Gestion des employeurs internes</p>
    <% if (stats) { %>
      <div class="stats-bar">
        <div class="stat-item">
          <div class="stat-value"><%= stats.total %></div>
          <div class="stat-label">Total</div>
        </div>
        <% if (stats.parGrade) { %>
          <% Object.entries(stats.parGrade).slice(0, 5).forEach(([grade, count]) => { %>
            <div class="stat-item stat-info">
              <div class="stat-value"><%= count %></div>
              <div class="stat-label"><%= grade %></div>
            </div>
          <% }); %>
        <% } %>
      </div>
    <% } %>
  </div>

  <!-- Formulaire d'ajout -->
  <% if (user) { %>
    <div class="card wide add-form">
      <h3>➕ Ajouter un employeur</h3>
      <form method="post" action="/employeurs">
        <% if (authToken) { %>
          <input type="hidden" name="token" value="<%= authToken %>" />
        <% } %>
        <div class="form-grid">
          <div class="form-group">
            <label>Prénom *</label>
            <input name="prenom" placeholder="Prénom" required />
          </div>
          <div class="form-group">
            <label>Nom *</label>
            <input name="nom" placeholder="Nom" required />
          </div>
          <div class="form-group">
            <label>Email</label>
            <input name="email" type="email" placeholder="email@exemple.com" />
          </div>
          <div class="form-group">
            <label>Téléphone</label>
            <input name="telephone" type="tel" placeholder="+33 6 12 34 56 78" />
          </div>
          <div class="form-group">
            <label>Poste</label>
            <input name="poste" placeholder="Ex: Développeur, Manager..." />
          </div>
          <div class="form-group">
            <label>Département</label>
            <select name="departement">
              <option value="">-- Sélectionner --</option>
              <option value="Direction">Direction</option>
              <option value="Finances">Finances</option>
              <option value="RH">RH</option>
              <option value="Marketing">Marketing</option>
              <option value="Ventes">Ventes</option>
              <option value="IT">IT</option>
              <option value="Operations">Operations</option>
              <option value="Service Client">Service Client</option>
              <option value="Autre">Autre</option>
            </select>
          </div>
          <div class="form-group">
            <label>Salaire (€)</label>
            <input name="salaire" type="number" step="0.01" placeholder="35000" />
          </div>
          <div class="form-group">
            <label>Date d'entrée</label>
            <input name="dateEntree" type="date" />
          </div>
          <div class="form-group">
            <label>Grade</label>
            <select name="grade">
              <option value="">-- Sélectionner --</option>
              <option value="Employé">Employé</option>
              <option value="Manager">Manager</option>
              <option value="Responsable Sécurité">Responsable Sécurité</option>
              <option value="Chef d'Équipe">Chef d'Équipe</option>
              <option value="Directeur">Directeur</option>
              <option value="Cadre Supérieur">Cadre Supérieur</option>
              <option value="Autre">Autre</option>
            </select>
          </div>
          <div class="form-group">
            <label>URL de la photo</label>
            <input name="photoUrl" placeholder="https://..." />
          </div>
          <div class="form-group full-width">
            <label>Notes</label>
            <textarea name="notes" placeholder="Notes additionnelles..." rows="3"></textarea>
          </div>
        </div>
        <button class="btn btn-add" type="submit">✅ Ajouter l'employeur</button>
      </form>
    </div>
  <% } %>

  <!-- Liste des employeurs -->
  <div class="list-section">
    <div class="list-header">
      <h3>Employeurs (<span class="badge"><%= items.length %></span>)</h3>
    </div>
    <div class="list employee-grid">
      <% if (items.length === 0) { %>
        <p class="empty-state">📭 Aucun employeur trouvé</p>
      <% } else { %>
        <% items.forEach(item => { %>
          <div class="employee-card" data-id="<%= item._id %>">
            <!-- Photo -->
            <div class="employee-photo-container">
              <% if (item.photoUrl) { %>
                <img src="<%= item.photoUrl %>" alt="<%= item.prenom %> <%= item.nom %>" class="employee-photo" onerror="this.style.display='none'; this.nextElementSibling.style.display='flex';" />
                <div class="employee-placeholder" style="display: none;">🧑‍💼</div>
              <% } else { %>
                <div class="employee-placeholder">🧑‍💼</div>
              <% } %>
            </div>

            <!-- Contenu -->
            <div class="card-content">
              <h4><%= item.prenom %> <%= item.nom %></h4>
              
              <% if (item.grade) { %>
                <p class="info-item grade-badge">⭐ <%= item.grade %></p>
              <% } %>
              
              <% if (item.poste) { %>
                <p class="info-item">💼 <%= item.poste %></p>
              <% } %>
              
              <% if (item.departement) { %>
                <p class="info-item">🏢 <%= item.departement %></p>
              <% } %>
              
              <% if (item.email) { %>
                <p class="info-item">📧 <a href="mailto:<%= item.email %>"><%= item.email %></a></p>
              <% } %>
              
              <% if (item.telephone) { %>
                <p class="info-item">📱 <%= item.telephone %></p>
              <% } %>
              
              <% if (item.dateEntree) { %>
                <p class="info-item">📅 Depuis le <%= new Date(item.dateEntree).toLocaleDateString('fr-FR') %></p>
              <% } %>
              
              <% if (item.salaire) { %>
                <p class="info-item">💰 <%= item.salaire.toLocaleString('fr-FR') %> €</p>
              <% } %>

              <!-- Actions (visible au survol) -->
              <% if (user) { %>
                <div class="action-buttons">
                  <button class="btn-edit" onclick="openEditModal('<%= item._id %>')">✏️ Modifier</button>
                  <form method="post" action="/employeurs/<%= item._id %>/delete" style="display: inline;">
                    <% if (authToken) { %>
                      <input type="hidden" name="token" value="<%= authToken %>" />
                    <% } %>
                    <button class="btn-delete" type="submit" onclick="return confirm('Voulez-vous vraiment supprimer cet employeur ?')">🗑️ Supprimer</button>
                  </form>
                </div>
              <% } %>
            </div>
          </div>
        <% }); %>
      <% } %>
    </div>
  </div>
</div>

<!-- Modal d'édition -->
<% if (user) { %>
  <div id="editModal" class="modal">
    <div class="modal-content">
      <span class="modal-close" onclick="closeEditModal()">&times;</span>
      <h2>✏️ Modifier l'employeur</h2>
      <form id="editForm" method="post">
        <% if (authToken) { %>
          <input type="hidden" name="token" value="<%= authToken %>" />
        <% } %>
        <div class="form-grid">
          <div class="form-group">
            <label>Prénom *</label>
            <input name="prenom" id="edit_prenom" required />
          </div>
          <div class="form-group">
            <label>Nom *</label>
            <input name="nom" id="edit_nom" required />
          </div>
          <div class="form-group">
            <label>Email</label>
            <input name="email" id="edit_email" type="email" />
          </div>
          <div class="form-group">
            <label>Téléphone</label>
            <input name="telephone" id="edit_telephone" type="tel" />
          </div>
          <div class="form-group">
            <label>Poste</label>
            <input name="poste" id="edit_poste" />
          </div>
          <div class="form-group">
            <label>Département</label>
            <select name="departement" id="edit_departement">
              <option value="">-- Sélectionner --</option>
              <option value="Direction">Direction</option>
              <option value="Finances">Finances</option>
              <option value="RH">RH</option>
              <option value="Marketing">Marketing</option>
              <option value="Ventes">Ventes</option>
              <option value="IT">IT</option>
              <option value="Operations">Operations</option>
              <option value="Service Client">Service Client</option>
              <option value="Autre">Autre</option>
            </select>
          </div>
          <div class="form-group">
            <label>Salaire (€)</label>
            <input name="salaire" id="edit_salaire" type="number" step="0.01" />
          </div>
          <div class="form-group">
            <label>Date d'entrée</label>
            <input name="dateEntree" id="edit_dateEntree" type="date" />
          </div>
          <div class="form-group">
            <label>Date de départ</label>
            <input name="dateDepart" id="edit_dateDepart" type="date" />
          </div>
          <div class="form-group">
            <label>Grade</label>
            <select name="grade" id="edit_grade">
              <option value="">-- Sélectionner --</option>
              <option value="Employé">Employé</option>
              <option value="Manager">Manager</option>
              <option value="Responsable Sécurité">Responsable Sécurité</option>
              <option value="Chef d'Équipe">Chef d'Équipe</option>
              <option value="Directeur">Directeur</option>
              <option value="Cadre Supérieur">Cadre Supérieur</option>
              <option value="Autre">Autre</option>
            </select>
          </div>
          <div class="form-group full-width">
            <label>URL de la photo</label>
            <input name="photoUrl" id="edit_photoUrl" />
          </div>
          <div class="form-group full-width">
            <label>Notes</label>
            <textarea name="notes" id="edit_notes" rows="3"></textarea>
          </div>
        </div>
        <div class="modal-actions">
          <button type="button" class="btn-cancel" onclick="closeEditModal()">Annuler</button>
          <button type="submit" class="btn btn-add">💾 Enregistrer</button>
        </div>
      </form>
    </div>
  </div>
<% } %>

<script>
// Ouvrir le modal d'édition
async function openEditModal(id) {
  try {
    const response = await fetch(`/employeurs/${id}`);
    const data = await response.json();
    
    // Remplir le formulaire
    document.getElementById('edit_prenom').value = data.prenom || '';
    document.getElementById('edit_nom').value = data.nom || '';
    document.getElementById('edit_email').value = data.email || '';
    document.getElementById('edit_telephone').value = data.telephone || '';
    document.getElementById('edit_poste').value = data.poste || '';
    document.getElementById('edit_departement').value = data.departement || '';
    document.getElementById('edit_grade').value = data.grade || '';
    document.getElementById('edit_salaire').value = data.salaire || '';
    document.getElementById('edit_dateEntree').value = data.dateEntree ? new Date(data.dateEntree).toISOString().split('T')[0] : '';
    document.getElementById('edit_dateDepart').value = data.dateDepart ? new Date(data.dateDepart).toISOString().split('T')[0] : '';
    document.getElementById('edit_photoUrl').value = data.photoUrl || '';
    document.getElementById('edit_notes').value = data.notes || '';
    
    // Définir l'action du formulaire
    document.getElementById('editForm').action = `/employeurs/${id}/update`;
    
    // Afficher le modal
    document.getElementById('editModal').style.display = 'block';
  } catch (error) {
    console.error('Erreur:', error);
    alert('Erreur lors du chargement des données');
  }
}

// Fermer le modal
function closeEditModal() {
  document.getElementById('editModal').style.display = 'none';
}

// Fermer le modal en cliquant en dehors
window.onclick = function(event) {
  const modal = document.getElementById('editModal');
  if (event.target === modal) {
    closeEditModal();
  }
}
</script>

<style>
/* === Toolbar et Recherche === */
.page-toolbar {
  margin-bottom: 25px;
}

.search-filter-form {
  display: flex;
  gap: 10px;
  flex-wrap: wrap;
  align-items: center;
}

.page-search-input {
  flex: 1;
  min-width: 250px;
  padding: 12px 18px;
  border: 1px solid rgba(59, 130, 246, 0.2);
  border-radius: 10px;
  background: rgba(59, 130, 246, 0.05);
  color: #e9eef3;
  font-size: 15px;
}

.filter-select {
  padding: 12px 18px;
  border: 1px solid rgba(59, 130, 246, 0.2);
  border-radius: 10px;
  background: rgba(59, 130, 246, 0.05);
  color: #e9eef3;
  font-size: 15px;
  cursor: pointer;
}

.filter-select option {
  background: #1a1a2e;
  color: #e9eef3;
}

.page-search-btn {
  padding: 12px 20px;
  background: linear-gradient(135deg, var(--accent), var(--accent-strong));
  border: none;
  border-radius: 10px;
  font-size: 18px;
  cursor: pointer;
  transition: transform 0.2s;
}

.page-search-btn:hover {
  transform: scale(1.05);
}

.btn-reset {
  padding: 12px 18px;
  background: rgba(255, 107, 107, 0.15);
  border: 1px solid rgba(255, 107, 107, 0.3);
  border-radius: 10px;
  color: #ff6b6b;
  text-decoration: none;
  font-size: 14px;
  transition: all 0.2s;
  white-space: nowrap;
}

.btn-reset:hover {
  background: rgba(255, 107, 107, 0.25);
}

/* === Header === */
.page-header {
  margin-bottom: 32px;
  text-align: center;
  border-bottom: 2px solid rgba(59, 130, 246, 0.18);
  padding-bottom: 24px;
}

.page-header h1 {
  margin: 0 0 10px 0;
  color: var(--accent);
  font-size: 38px;
  letter-spacing: 1px;
}

.page-header p {
  color: var(--muted);
  margin: 0 0 20px 0;
}

/* === Statistiques === */
.stats-bar {
  display: flex;
  justify-content: center;
  gap: 25px;
  margin-top: 20px;
  flex-wrap: wrap;
}

.stat-item {
  background: rgba(59, 130, 246, 0.1);
  border: 1px solid rgba(59, 130, 246, 0.2);
  border-radius: 12px;
  padding: 15px 30px;
  text-align: center;
  min-width: 100px;
}

.stat-success {
  background: rgba(76, 175, 80, 0.1);
  border-color: rgba(76, 175, 80, 0.3);
}

.stat-warning {
  background: rgba(255, 193, 7, 0.1);
  border-color: rgba(255, 193, 7, 0.3);
}

.stat-danger {
  background: rgba(244, 67, 54, 0.1);
  border-color: rgba(244, 67, 54, 0.3);
}

.stat-info {
  background: rgba(103, 58, 183, 0.1);
  border-color: rgba(103, 58, 183, 0.3);
}

.stat-value {
  font-size: 28px;
  font-weight: bold;
  color: var(--accent);
  margin-bottom: 5px;
}

.stat-success .stat-value {
  color: #4caf50;
}

.stat-warning .stat-value {
  color: #ffc107;
}

.stat-danger .stat-value {
  color: #f44336;
}

.stat-info .stat-value {
  color: #673ab7;
}

.stat-label {
  font-size: 13px;
  color: var(--muted);
  text-transform: uppercase;
  letter-spacing: 0.5px;
}

/* === Formulaire === */
.add-form {
  background: rgba(59, 130, 246, 0.10);
  border: 1px solid rgba(59, 130, 246, 0.22);
  margin-bottom: 32px;
  box-shadow: 0 4px 18px rgba(59, 130, 246, 0.08);
}

.add-form h3 {
  color: var(--accent);
  margin-top: 0;
  font-size: 22px;
}

.form-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
  gap: 18px;
  margin-bottom: 20px;
}

.form-group {
  display: flex;
  flex-direction: column;
}

.form-group.full-width {
  grid-column: 1 / -1;
}

.form-group label {
  margin-bottom: 8px;
  color: var(--accent);
  font-size: 14px;
  font-weight: 500;
}

.form-group input,
.form-group select,
.form-group textarea {
  padding: 12px;
  border: 1px solid rgba(59, 130, 246, 0.2);
  border-radius: 8px;
  background: rgba(59, 130, 246, 0.05);
  color: #e9eef3;
  font-size: 15px;
}

.form-group textarea {
  resize: vertical;
  font-family: inherit;
}

.btn-add {
  background: linear-gradient(135deg, var(--accent), var(--accent-strong));
  color: #000;
  font-weight: bold;
  padding: 13px 22px;
  border-radius: 10px;
  font-size: 16px;
  box-shadow: 0 2px 8px rgba(59, 130, 246, 0.08);
  border: none;
  cursor: pointer;
  transition: all 0.3s;
}

.btn-add:hover {
  transform: scale(1.04);
  box-shadow: 0 6px 18px rgba(59, 130, 246, 0.18);
}

/* === Liste === */
.list-section {
  margin-top: 32px;
}

.list-header {
  margin-bottom: 28px;
  padding-bottom: 14px;
  border-bottom: 1px solid rgba(59, 130, 246, 0.18);
}

.list-header h3 {
  margin: 0;
  color: var(--accent);
  display: flex;
  align-items: center;
  gap: 10px;
  font-size: 20px;
}

.badge {
  background: rgba(59, 130, 246, 0.13);
  color: var(--accent);
  padding: 2px 10px;
  border-radius: 20px;
  font-size: 15px;
  font-weight: bold;
}

.employee-grid {
  display: grid !important;
  grid-template-columns: repeat(auto-fill, minmax(320px, 1fr));
  gap: 24px;
  align-items: start;
}

/* === Carte employé === */
.employee-card {
  background: rgba(59, 130, 246, 0.07);
  border: 1px solid rgba(59, 130, 246, 0.13);
  border-radius: 16px;
  overflow: hidden;
  transition: all 0.3s ease;
  display: flex;
  flex-direction: column;
  height: 100%;
  box-shadow: 0 2px 12px rgba(59, 130, 246, 0.07);
}

.employee-card:hover {
  background: rgba(59, 130, 246, 0.13);
  border-color: rgba(59, 130, 246, 0.22);
  transform: translateY(-6px);
  box-shadow: 0 10px 30px rgba(59, 130, 246, 0.13);
}

.employee-photo-container {
  width: 100%;
  height: 200px;
  overflow: hidden;
  background: rgba(59, 130, 246, 0.13);
  position: relative;
}

.employee-photo {
  width: 100%;
  height: 100%;
  object-fit: cover;
}

.employee-placeholder {
  width: 100%;
  height: 100%;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 70px;
  background: rgba(59, 130, 246, 0.10);
}

/* Contenu carte */
.card-content {
  padding: 20px;
  flex: 1;
  display: flex;
  flex-direction: column;
}

.card-content h4 {
  margin: 0 0 14px 0;
  color: #e9eef3;
  font-size: 19px;
  font-weight: 600;
}

.info-item {
  margin: 8px 0;
  color: var(--muted);
  font-size: 14px;
  line-height: 1.5;
}

.info-item a {
  color: var(--accent);
  text-decoration: none;
}

.info-item a:hover {
  text-decoration: underline;
}

.grade-badge {
  background: linear-gradient(135deg, rgba(255, 193, 7, 0.15), rgba(255, 152, 0, 0.15));
  border: 1px solid rgba(255, 193, 7, 0.3);
  padding: 6px 12px;
  border-radius: 20px;
  display: inline-block;
  font-weight: 600;
  color: #ffc107;
  font-size: 13px;
}

/* Boutons d'action */
.action-buttons {
  margin-top: auto;
  padding-top: 15px;
  display: flex;
  gap: 10px;
  max-height: 0;
  overflow: hidden;
  opacity: 0;
  transition: max-height 0.3s ease, opacity 0.3s ease, padding 0.3s ease;
}

.employee-card:hover .action-buttons {
  max-height: 100px;
  opacity: 1;
  padding-top: 15px;
}

.btn-edit {
  flex: 1;
  background: rgba(59, 130, 246, 0.2);
  border: 1px solid rgba(59, 130, 246, 0.3);
  color: var(--accent);
  padding: 10px;
  border-radius: 8px;
  cursor: pointer;
  transition: all 0.2s;
  font-size: 14px;
  font-weight: 500;
}

.btn-edit:hover {
  background: rgba(59, 130, 246, 0.35);
  transform: translateY(-2px);
}

.btn-delete {
  flex: 1;
  background: rgba(255, 107, 107, 0.18);
  border: 1px solid rgba(255, 107, 107, 0.22);
  color: var(--danger);
  padding: 10px;
  border-radius: 8px;
  cursor: pointer;
  transition: all 0.2s;
  font-size: 14px;
  font-weight: 500;
}

.btn-delete:hover {
  background: rgba(255, 107, 107, 0.3);
  border-color: #ff6b6b;
  transform: translateY(-2px);
}

/* === Modal === */
.modal {
  display: none;
  position: fixed;
  z-index: 1000;
  left: 0;
  top: 0;
  width: 100%;
  height: 100%;
  background-color: rgba(0, 0, 0, 0.7);
  backdrop-filter: blur(5px);
  overflow-y: auto;
}

.modal-content {
  background: #1a1a2e;
  margin: 50px auto;
  padding: 30px;
  border: 1px solid rgba(59, 130, 246, 0.3);
  border-radius: 16px;
  width: 90%;
  max-width: 800px;
  box-shadow: 0 10px 50px rgba(0, 0, 0, 0.5);
  position: relative;
}

.modal-content h2 {
  color: var(--accent);
  margin-top: 0;
  margin-bottom: 25px;
}

.modal-close {
  position: absolute;
  right: 20px;
  top: 20px;
  color: #aaa;
  font-size: 32px;
  font-weight: bold;
  cursor: pointer;
  transition: color 0.2s;
}

.modal-close:hover {
  color: #fff;
}

.modal-actions {
  display: flex;
  gap: 15px;
  justify-content: flex-end;
  margin-top: 25px;
}

.btn-cancel {
  padding: 12px 24px;
  background: rgba(128, 128, 128, 0.2);
  border: 1px solid rgba(128, 128, 128, 0.3);
  color: #aaa;
  border-radius: 8px;
  cursor: pointer;
  transition: all 0.2s;
  font-size: 15px;
}

.btn-cancel:hover {
  background: rgba(128, 128, 128, 0.3);
  color: #fff;
}

/* === Empty State === */
.empty-state {
  text-align: center;
  color: #bdbdbd;
  padding: 40px;
  grid-column: 1 / -1;
  font-size: 16px;
}

/* === Responsive === */
@media (max-width: 768px) {
  .search-filter-form {
    flex-direction: column;
  }
  
  .page-search-input,
  .filter-select {
    width: 100%;
  }
  
  .stats-bar {
    gap: 15px;
  }
  
  .stat-item {
    padding: 12px 20px;
    min-width: 80px;
  }
  
  .employee-grid {
    grid-template-columns: 1fr;
  }
  
  .form-grid {
    grid-template-columns: 1fr;
  }
  
  .modal-content {
    width: 95%;
    padding: 20px;
    margin: 30px auto;
  }
}
</style>

<%- include('partials/footer') %>

</body></html>

<style>
.page-header {
  margin-bottom: 32px;
  text-align: center;
  border-bottom: 2px solid rgba(59, 130, 246, 0.18);
  padding-bottom: 18px;
}

.page-header h1 {
  margin: 0 0 10px 0;
  color: var(--accent);
  font-size: 38px;
  letter-spacing: 1px;
}

.page-header p {
  color: var(--muted);
  margin: 0;
}

.add-form {
  background: rgba(59, 130, 246, 0.10);
  border: 1px solid rgba(59, 130, 246, 0.22);
  margin-bottom: 32px;
  box-shadow: 0 4px 18px rgba(59, 130, 246, 0.08);
}

.add-form h3 {
  color: var(--accent);
  margin-top: 0;
  font-size: 22px;
}

.btn-add {
  background: linear-gradient(135deg, var(--accent), var(--accent-strong));
  color: #000;
  font-weight: bold;
  padding: 13px 22px;
  border-radius: 10px;
  font-size: 16px;
  box-shadow: 0 2px 8px rgba(59, 130, 246, 0.08);
}

.btn-add:hover {
  transform: scale(1.04);
  box-shadow: 0 6px 18px rgba(59, 130, 246, 0.18);
}

.list-section {
  margin-top: 32px;
}

.list-header {
  margin-bottom: 28px;
  padding-bottom: 14px;
  border-bottom: 1px solid rgba(59, 130, 246, 0.18);
}

.list-header h3 {
  margin: 0;
  color: var(--accent);
  display: flex;
  align-items: center;
  gap: 10px;
  font-size: 20px;
}

.badge {
  background: rgba(59, 130, 246, 0.13);
  color: var(--accent);
  padding: 2px 10px;
  border-radius: 20px;
  font-size: 15px;
  font-weight: bold;
}

.employee-grid {
  display: grid !important;
  grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
  gap: 24px;
  align-items: start;
}

.employee-card {
  background: rgba(59, 130, 246, 0.07);
  border: 1px solid rgba(59, 130, 246, 0.13);
  border-radius: 16px;
  overflow: hidden;
  transition: all 0.3s ease;
  display: flex;
  flex-direction: column;
  height: 100%;
  box-shadow: 0 2px 12px rgba(59, 130, 246, 0.07);
}

.employee-card:hover {
  background: rgba(59, 130, 246, 0.13);
  border-color: rgba(59, 130, 246, 0.22);
  transform: translateY(-6px);
  box-shadow: 0 10px 30px rgba(59, 130, 246, 0.13);
}

.employee-photo-container {
  width: 100%;
  height: 180px;
  overflow: hidden;
  background: rgba(59, 130, 246, 0.13);
}

.employee-photo {
  width: 100%;
  height: 100%;
  object-fit: cover;
}

.employee-placeholder {
  width: 100%;
  height: 100%;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 60px;
  background: rgba(59, 130, 246, 0.10);
}

.card-content {
  padding: 18px;
  flex: 1;
  display: flex;
  flex-direction: column;
}

.card-content h4 {
  margin: 0 0 12px 0;
  color: #e9eef3;
  font-size: 18px;
}

.info-item {
  margin: 10px 0;
  color: var(--muted);
  font-size: 15px;
}

.delete-btn-container {
  margin-top: auto;
  max-height: 0;
  overflow: hidden;
  opacity: 0;
  transition: max-height 0.25s ease, opacity 0.25s ease;
}

.employee-card:hover .delete-btn-container {
  max-height: 80px;
  opacity: 1;
}

.btn-delete {
  width: 100%;
  background: rgba(255, 107, 107, 0.18);
  border: 1px solid rgba(255, 107, 107, 0.22);
  color: var(--danger);
  padding: 10px;
  border-radius: 10px;
  cursor: pointer;
  transition: background 0.2s, border-color 0.2s;
  margin-top: 12px;
  font-size: 15px;
}

.btn-delete:hover {
  background: rgba(255, 107, 107, 0.3);
  border-color: #ff6b6b;
}

.empty-state {
  text-align: center;
  color: #bdbdbd;
  padding: 40px;
  grid-column: 1 / -1;
}

.login-cta {
  background: rgba(59, 130, 246, 0.08);
  border: 2px solid rgba(59, 130, 246, 0.3);
  border-radius: 12px;
  padding: 30px;
  text-align: center;
  margin-top: 30px;
}

.login-cta p {
  color: #bdbdbd;
  margin: 0 0 15px 0;
}

.btn-primary {
  background: linear-gradient(135deg, #3b82f6, #60a5fa);
  color: #000;
  padding: 10px 25px;
  border-radius: 6px;
  font-weight: bold;
}

.btn-primary:hover {
  transform: scale(1.05);
}
</style>

<%- include('partials/footer') %>

</body></html>
