<%- include('partials/header') %>
<%- include('partials/nav') %>

<div class="container page-shell executive-suite">
  <!-- Barre de recherche premium -->
  <div class="page-toolbar">
    <form method="get" action="/employeurs" class="search-filter-form">
      <input type="text" name="search" class="page-search-input" placeholder="🔍 Rechercher un employé..." value="<%= filters?.search || '' %>" />
      <button class="page-search-btn" type="submit">
        <span class="search-icon">⚡</span>
        <span class="search-text">Rechercher</span>
      </button>
      <% if (filters?.search) { %>
        <a href="/employeurs" class="btn-reset">✖ Effacer</a>
      <% } %>
    </form>
  </div>

  <!-- En-tête avec statistiques premium -->
  <div class="page-header">
    <div class="header-glow"></div>
    <h1 class="executive-title">
      <span class="title-icon">👑</span>
      <span class="title-text">Employés</span>
      <span class="title-icon">👑</span>
    </h1>
    <p class="executive-subtitle">L'Excellence de notre Casino</p>
    <% if (stats) { %>
      <div class="stats-bar">
        <div class="stat-item stat-primary">
          <div class="stat-icon">💎</div>
          <div class="stat-value"><%= stats.total %></div>
          <div class="stat-label">Employés</div>
          <div class="stat-shine"></div>
        </div>
        <% if (stats.parGrade) { %>
          <% Object.entries(stats.parGrade).slice(0, 4).forEach(([grade, count]) => { %>
            <div class="stat-item stat-gradient">
              <div class="stat-icon">⭐</div>
              <div class="stat-value"><%= count %></div>
              <div class="stat-label"><%= grade %></div>
              <div class="stat-shine"></div>
            </div>
          <% }); %>
        <% } %>
      </div>
    <% } %>
  </div>

  <!-- Formulaire premium d'ajout -->
  <% if (user) { %>
    <div class="card wide add-form executive-form">
      <div class="form-header">
        <div class="form-icon-premium">✨</div>
        <h3>Ajouter un Employés</h3>
        <div class="form-decoration-line"></div>
      </div>
      <form method="post" action="/employeurs">
        <% if (authToken) { %>
          <input type="hidden" name="token" value="<%= authToken %>" />
        <% } %>
        <div class="form-grid-premium">
          <div class="form-group-premium">
            <label>
              <span class="label-icon">👤</span>
              <span class="label-text">Prénom *</span>
            </label>
            <input name="prenom" placeholder="Entrez le prénom" required />
            <div class="input-underline"></div>
          </div>
          <div class="form-group-premium">
            <label>
              <span class="label-icon">📛</span>
              <span class="label-text">Nom *</span>
            </label>
            <input name="nom" placeholder="Entrez le nom" required />
            <div class="input-underline"></div>
          </div>
          <div class="form-group-premium">
            <label>
              <span class="label-icon">🎖️</span>
              <span class="label-text">Grade</span>
            </label>
            <select name="grade">
              <option value="">-- Sélectionner un grade --</option>
              <option value="Manager">Manager</option>
              <option value="Responsable Sécurité">Responsable Sécurité</option>
              <option value="Adjoint de direction">Adjoint de direction</option>
              <option value="Co Directrice">Co Directrice</option>
              <option value="Directeur">Directeur</option>
              <option value="Autre">Autre</option>
            </select>
            <div class="input-underline"></div>
          </div>
          <div class="form-group-premium">
            <label>
              <span class="label-icon">📸</span>
              <span class="label-text">URL Photo</span>
            </label>
            <input name="photoUrl" placeholder="https://..." />
            <div class="input-underline"></div>
          </div>
        </div>
        <button class="btn btn-add-premium" type="submit">
          <span class="btn-glow"></span>
          <span class="btn-content">
            <span class="btn-icon">✅</span>
            <span class="btn-text">Ajouter au Employés</span>
          </span>
        </button>
      </form>
    </div>
  <% } %>

  <!-- Liste des membres de la direction avec cartes 3D -->
  <div class="list-section">
    <div class="list-header-premium">
      <div class="header-decoration-left"></div>
      <h3>
        <span class="list-icon">🌟</span>
        Nos Leaders
        <span class="badge-premium"><%= items.length %></span>
      </h3>
      <div class="header-decoration-right"></div>
    </div>
    
    <div class="executive-grid">
      <% if (items.length === 0) { %>
        <div class="empty-state-premium">
          <div class="empty-icon">👔</div>
          <p>Aucun membre de la direction trouvé</p>
          <div class="empty-decoration"></div>
        </div>
      <% } else { %>
        <% items.forEach((item, index) => { %>
          <div class="executive-card-wrapper" style="--card-delay: <%= index %>;">
            <div class="executive-card" data-id="<%= item._id %>">
              
              <!-- Photo avec effets premium -->
              <div class="executive-photo-container">
                <% if (item.photoUrl) { %>
                  <img src="<%= item.photoUrl %>" alt="<%= item.prenom %> <%= item.nom %>" class="executive-photo" onerror="this.style.display='none'; this.nextElementSibling.style.display='flex';" />
                  <div class="executive-placeholder" style="display: none;">
                    <span class="placeholder-icon">👤</span>
                  </div>
                <% } else { %>
                  <div class="executive-placeholder">
                    <span class="placeholder-icon">👤</span>
                  </div>
                <% } %>
                <div class="photo-overlay"></div>
              </div>

              <!-- Informations principales -->
              <div class="card-content-premium">
                <div class="executive-rank-badge">
                  <span class="rank-icon">⭐</span>
                  <span class="rank-text"><%= item.grade || 'Non spécifié' %></span>
                  <div class="badge-shine"></div>
                </div>
                
                <h4 class="executive-name">
                  <span class="name-first"><%= item.prenom %></span>
                  <span class="name-last"><%= item.nom %></span>
                </h4>
                
                <div class="card-status-indicator">
                  <span class="status-dot"></span>
                  <span class="status-text">Membre Actif</span>
                </div>
              </div>
              
              <!-- Panneau d'actions qui glisse depuis le bas -->
              <% if (user) { %>
                <div class="action-panel-slide">
                  <div class="action-panel-header">
                    <div class="action-icon-main">⚡</div>
                    <h5>Actions Rapides</h5>
                  </div>
                  
                  <div class="action-buttons-slide">
                    <button class="btn-action-slide btn-edit-slide" onclick="openEditModal('<%= item._id %>')">
                      <span class="btn-slide-icon">✏️</span>
                      <span class="btn-slide-text">Modifier</span>
                      <div class="btn-slide-glow"></div>
                    </button>
                    <form method="post" action="/employeurs/<%= item._id %>/delete" style="width: 100%;">
                      <% if (authToken) { %>
                        <input type="hidden" name="token" value="<%= authToken %>" />
                      <% } %>
                      <button class="btn-action-slide btn-delete-slide" type="submit" onclick="return confirm('⚠️ Supprimer <%= item.prenom %> <%= item.nom %> ?')">
                        <span class="btn-slide-icon">🗑️</span>
                        <span class="btn-slide-text">Supprimer</span>
                        <div class="btn-slide-glow"></div>
                      </button>
                    </form>
                  </div>
                </div>
              <% } %>
              
              <div class="card-corner-decor top-left"></div>
              <div class="card-corner-decor top-right"></div>
              <div class="card-corner-decor bottom-left"></div>
              <div class="card-corner-decor bottom-right"></div>
            </div>
          </div>
        <% }); %>
      <% } %>
    </div>
  </div>
</div>

<!-- Modal d'édition premium -->
<% if (user) { %>
  <div id="editModal" class="modal-premium">
    <div class="modal-backdrop-blur"></div>
    <div class="modal-content-premium">
      <div class="modal-glow-effect"></div>
      <span class="modal-close-premium" onclick="closeEditModal()">
        <span class="close-icon">✖</span>
      </span>
      
      <div class="modal-header-premium">
        <div class="modal-icon-premium">✨</div>
        <h2>Modifier le Profil</h2>
        <div class="modal-decoration"></div>
      </div>
      
      <form id="editForm" method="post">
        <% if (authToken) { %>
          <input type="hidden" name="token" value="<%= authToken %>" />
        <% } %>
        <div class="form-grid-premium">
          <div class="form-group-premium">
            <label>
              <span class="label-icon">👤</span>
              <span class="label-text">Prénom *</span>
            </label>
            <input name="prenom" id="edit_prenom" required />
            <div class="input-underline"></div>
          </div>
          <div class="form-group-premium">
            <label>
              <span class="label-icon">📛</span>
              <span class="label-text">Nom *</span>
            </label>
            <input name="nom" id="edit_nom" required />
            <div class="input-underline"></div>
          </div>
          <div class="form-group-premium">
            <label>
              <span class="label-icon">🎖️</span>
              <span class="label-text">Grade</span>
            </label>
            <select name="grade" id="edit_grade">
              <option value="">-- Sélectionner un grade --</option>
              <option value="Manager">Manager</option>
              <option value="Responsable Sécurité">Responsable Sécurité</option>
              <option value="Adjoint de direction">Adjoint de direction</option>
              <option value="Co directrice">Co directrice</option>
              <option value="Directeur">Directeur</option>
              <option value="Autre">Autre</option>
            </select>
            <div class="input-underline"></div>
          </div>
          <div class="form-group-premium">
            <label>
              <span class="label-icon">📸</span>
              <span class="label-text">URL Photo</span>
            </label>
            <input name="photoUrl" id="edit_photoUrl" />
            <div class="input-underline"></div>
          </div>
        </div>
        <div class="modal-actions-premium">
          <button type="button" class="btn-cancel-premium" onclick="closeEditModal()">
            <span class="btn-icon">❌</span>
            <span>Annuler</span>
          </button>
          <button type="submit" class="btn-save-premium">
            <span class="btn-glow"></span>
            <span class="btn-content">
              <span class="btn-icon">💾</span>
              <span>Enregistrer</span>
            </span>
          </button>
        </div>
      </form>
    </div>
  </div>
<% } %>

<script>
// Ouvrir le modal d'édition
async function openEditModal(id) {
  try {
    const response = await fetch(`/employeurs/${id}`);
    const data = await response.json();
    
    // Remplir le formulaire
    document.getElementById('edit_prenom').value = data.prenom || '';
    document.getElementById('edit_nom').value = data.nom || '';
    document.getElementById('edit_grade').value = data.grade || '';
    document.getElementById('edit_photoUrl').value = data.photoUrl || '';
    
    // Définir l'action du formulaire
    document.getElementById('editForm').action = `/employeurs/${id}/update`;
    
    // Afficher le modal avec animation
    const modal = document.getElementById('editModal');
    modal.style.display = 'flex';
    setTimeout(() => modal.classList.add('modal-show'), 10);
  } catch (error) {
    console.error('Erreur:', error);
    alert('❌ Erreur lors du chargement des données');
  }
}

// Fermer le modal
function closeEditModal() {
  const modal = document.getElementById('editModal');
  modal.classList.remove('modal-show');
  setTimeout(() => modal.style.display = 'none', 300);
}

// Fermer le modal en cliquant sur le fond
window.onclick = function(event) {
  const modal = document.getElementById('editModal');
  if (event.target === modal) {
    closeEditModal();
  }
}

// Animation des particules
document.addEventListener('DOMContentLoaded', () => {
  const particles = document.querySelectorAll('.particle');
  particles.forEach((particle, index) => {
    particle.style.left = Math.random() * 100 + '%';
    particle.style.animationDelay = Math.random() * 10 + 's';
    particle.style.animationDuration = (15 + Math.random() * 10) + 's';
  });
});
</script>

<style>
/* ========================================
   🎨 VARIABLES ET CONFIGURATION
   ======================================== */
:root {
  --primary: #4f46e5;
  --primary-dark: #4338ca;
  --secondary: #8b5cf6;
  --accent: #60a5fa;
  --gold: #f59e0b;
  --success: #10b981;
  --danger: #ef4444;
  --dark-bg: rgba(15, 23, 42, 0.95);
  --card-bg: rgba(30, 41, 59, 0.8);
  --glass-bg: rgba(51, 65, 85, 0.5);
}

/* ========================================
   ✨ ANIMATIONS SIMPLES
   ======================================== */
@keyframes fadeInUp {
  from {
    opacity: 0;
    transform: translateY(30px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

@keyframes scaleIn {
  from {
    opacity: 0;
    transform: scale(0.9);
  }
  to {
    opacity: 1;
    transform: scale(1);
  }
}

/* ========================================
   📱 CONTENEUR PRINCIPAL
   ======================================== */
.executive-suite {
  position: relative;
  z-index: 1;
}

/* ========================================
   🔍 BARRE DE RECHERCHE
   ======================================== */
.page-toolbar {
  margin-bottom: 30px;
  animation: fadeInUp 0.6s ease-out;
}

.search-filter-form {
  display: flex;
  gap: 12px;
  align-items: center;
  padding: 20px;
  background: var(--card-bg);
  border-radius: 15px;
  border: 1px solid rgba(100, 116, 139, 0.3);
  backdrop-filter: blur(10px);
  box-shadow: 0 4px 20px rgba(0, 0, 0, 0.2);
}

.page-search-input {
  flex: 1;
  min-width: 250px;
  padding: 14px 20px;
  border: 1px solid rgba(100, 116, 139, 0.3);
  border-radius: 10px;
  background: rgba(15, 23, 42, 0.5);
  color: #ffffff;
  font-size: 15px;
  transition: all 0.3s ease;
}

.page-search-input::placeholder {
  color: rgba(255, 255, 255, 0.5);
}

.page-search-input:focus {
  outline: none;
  border-color: var(--primary);
  background: rgba(15, 23, 42, 0.7);
  box-shadow: 0 0 0 3px rgba(79, 70, 229, 0.2);
}

.page-search-btn {
  padding: 14px 28px;
  background: var(--primary);
  border: none;
  border-radius: 10px;
  color: #ffffff;
  font-size: 15px;
  font-weight: 600;
  cursor: pointer;
  transition: all 0.3s ease;
  display: flex;
  align-items: center;
  gap: 8px;
}

.page-search-btn:hover {
  background: var(--primary-dark);
  transform: translateY(-2px);
  box-shadow: 0 6px 20px rgba(79, 70, 229, 0.4);
}

.search-icon {
  font-size: 18px;
}

.btn-reset {
  padding: 14px 20px;
  background: rgba(239, 68, 68, 0.2);
  border: 1px solid rgba(239, 68, 68, 0.4);
  border-radius: 10px;
  color: #ef4444;
  text-decoration: none;
  font-size: 14px;
  font-weight: 600;
  transition: all 0.3s ease;
}

.btn-reset:hover {
  background: rgba(239, 68, 68, 0.3);
  transform: translateY(-2px);
}

/* ========================================
   👑 EN-TÊTE
   ======================================== */
.page-header {
  margin-bottom: 40px;
  text-align: center;
  padding: 50px 30px;
  background: var(--card-bg);
  border-radius: 20px;
  border: 1px solid rgba(100, 116, 139, 0.3);
  backdrop-filter: blur(10px);
  box-shadow: 0 8px 30px rgba(0, 0, 0, 0.3);
  animation: fadeInUp 0.6s ease-out 0.1s both;
}

.header-glow {
  display: none;
}

.executive-title {
  margin: 0 0 15px 0;
  font-size: 42px;
  font-weight: 800;
  letter-spacing: 2px;
  text-transform: uppercase;
  color: #ffffff;
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 15px;
}

.title-icon {
  font-size: 40px;
}

.executive-subtitle {
  color: rgba(255, 255, 255, 0.7);
  font-size: 18px;
  font-weight: 500;
  letter-spacing: 2px;
  text-transform: uppercase;
  margin: 0 0 30px 0;
}

/* ========================================
   📊 STATISTIQUES
   ======================================== */
.stats-bar {
  display: flex;
  justify-content: center;
  gap: 20px;
  flex-wrap: wrap;
}

.stat-item {
  background: rgba(15, 23, 42, 0.6);
  border: 1px solid rgba(100, 116, 139, 0.3);
  border-radius: 15px;
  padding: 20px 30px;
  min-width: 120px;
  text-align: center;
  transition: all 0.3s ease;
}

.stat-primary {
  border-color: var(--gold);
}

.stat-gradient {
  border-color: var(--accent);
}

.stat-item:hover {
  transform: translateY(-5px);
  box-shadow: 0 10px 30px rgba(0, 0, 0, 0.4);
}

.stat-icon {
  font-size: 28px;
  margin-bottom: 8px;
}

.stat-value {
  font-size: 36px;
  font-weight: 800;
  color: #ffffff;
  margin-bottom: 6px;
}

.stat-label {
  font-size: 12px;
  color: rgba(255, 255, 255, 0.6);
  text-transform: uppercase;
  letter-spacing: 1px;
  font-weight: 600;
}

.stat-shine {
  display: none;
}

/* ========================================
   📝 FORMULAIRE
   ======================================== */
.executive-form {
  background: var(--card-bg);
  border: 1px solid rgba(100, 116, 139, 0.3);
  margin-bottom: 40px;
  border-radius: 20px;
  padding: 35px;
  backdrop-filter: blur(10px);
  box-shadow: 0 8px 30px rgba(0, 0, 0, 0.3);
  animation: fadeInUp 0.6s ease-out 0.2s both;
}

.form-header {
  text-align: center;
  margin-bottom: 30px;
}

.form-icon-premium {
  font-size: 40px;
  margin-bottom: 12px;
}

.form-header h3 {
  color: #ffffff;
  font-size: 24px;
  font-weight: 700;
  letter-spacing: 1px;
  margin: 0 0 12px 0;
}

.form-decoration-line {
  width: 80px;
  height: 3px;
  background: var(--primary);
  margin: 0 auto;
  border-radius: 2px;
}

.form-grid-premium {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
  gap: 20px;
  margin-bottom: 25px;
}

.form-group-premium {
  position: relative;
}

.form-group-premium label {
  display: flex;
  align-items: center;
  gap: 8px;
  margin-bottom: 10px;
  color: rgba(255, 255, 255, 0.9);
  font-size: 13px;
  font-weight: 600;
  letter-spacing: 0.5px;
  text-transform: uppercase;
}

.label-icon {
  font-size: 16px;
}

.form-group-premium input,
.form-group-premium select {
  width: 100%;
  padding: 14px 16px;
  border: 1px solid rgba(100, 116, 139, 0.3);
  border-radius: 10px;
  background: rgba(15, 23, 42, 0.5);
  color: #ffffff;
  font-size: 15px;
  transition: all 0.3s ease;
}

.form-group-premium input:focus,
.form-group-premium select:focus {
  outline: none;
  border-color: var(--primary);
  background: rgba(15, 23, 42, 0.7);
  box-shadow: 0 0 0 3px rgba(79, 70, 229, 0.2);
}

.input-underline {
  display: none;
}

.btn-add-premium {
  width: 100%;
  padding: 16px 32px;
  background: var(--primary);
  border: none;
  border-radius: 10px;
  color: #ffffff;
  font-size: 16px;
  font-weight: 700;
  text-transform: uppercase;
  letter-spacing: 1px;
  cursor: pointer;
  transition: all 0.3s ease;
}

.btn-add-premium:hover {
  background: var(--primary-dark);
  transform: translateY(-2px);
  box-shadow: 0 8px 25px rgba(79, 70, 229, 0.4);
}

.btn-glow {
  display: none;
}

.btn-content {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 10px;
}

.btn-icon {
  font-size: 20px;
}

/* ========================================
   🎴 CARTES EXECUTIVES
   ======================================== */
.list-section {
  animation: fadeInUp 0.6s ease-out 0.3s both;
}

.list-header-premium {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 15px;
  margin-bottom: 35px;
  padding: 20px;
  background: var(--card-bg);
  border-radius: 15px;
  border: 1px solid rgba(100, 116, 139, 0.3);
  backdrop-filter: blur(10px);
}

.header-decoration-left,
.header-decoration-right {
  flex: 1;
  height: 2px;
  background: linear-gradient(90deg, transparent, var(--primary), transparent);
}

.list-header-premium h3 {
  margin: 0;
  color: #ffffff;
  font-size: 26px;
  font-weight: 700;
  display: flex;
  align-items: center;
  gap: 12px;
}

.list-icon {
  font-size: 28px;
}

.badge-premium {
  background: var(--primary);
  color: #ffffff;
  padding: 6px 16px;
  border-radius: 20px;
  font-size: 16px;
  font-weight: 700;
}

.executive-grid {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(320px, 1fr));
  gap: 30px;
}

.executive-card-wrapper {
  animation: scaleIn 0.5s ease-out both;
  animation-delay: calc(var(--card-delay, 0) * 0.1s);
}

/* Carte principale */
.executive-card {
  position: relative;
  height: 520px;
  border-radius: 20px;
  overflow: hidden;
  background: var(--card-bg);
  border: 1px solid rgba(100, 116, 139, 0.3);
  backdrop-filter: blur(10px);
  cursor: pointer;
  transition: all 0.4s ease;
  display: flex;
  flex-direction: column;
  box-shadow: 0 8px 30px rgba(0, 0, 0, 0.3);
}

.executive-card:hover {
  transform: translateY(-15px);
  box-shadow: 0 20px 50px rgba(0, 0, 0, 0.5);
  border-color: var(--primary);
}

/* Photo */
.executive-photo-container {
  position: relative;
  width: 100%;
  height: 280px;
  overflow: hidden;
  background: linear-gradient(135deg, rgba(79, 70, 229, 0.2), rgba(139, 92, 246, 0.2));
}

.executive-photo {
  width: 100%;
  height: 100%;
  object-fit: cover;
  transition: transform 0.4s ease;
}

.executive-card:hover .executive-photo {
  transform: scale(1.1);
}

.executive-placeholder {
  width: 100%;
  height: 100%;
  display: flex;
  align-items: center;
  justify-content: center;
  background: linear-gradient(135deg, rgba(79, 70, 229, 0.3), rgba(139, 92, 246, 0.3));
}

.placeholder-icon {
  font-size: 100px;
  opacity: 0.5;
}

.photo-overlay {
  position: absolute;
  bottom: 0;
  left: 0;
  right: 0;
  height: 40%;
  background: linear-gradient(to top, rgba(0, 0, 0, 0.7), transparent);
  pointer-events: none;
}

/* Contenu */
.card-content-premium {
  flex: 1;
  padding: 25px;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  text-align: center;
}

.executive-rank-badge {
  display: inline-flex;
  align-items: center;
  gap: 6px;
  padding: 8px 20px;
  background: var(--gold);
  border-radius: 20px;
  margin-bottom: 15px;
}

.rank-icon {
  font-size: 16px;
}

.rank-text {
  color: #000;
  font-size: 13px;
  font-weight: 800;
  text-transform: uppercase;
  letter-spacing: 0.5px;
}

.badge-shine {
  display: none;
}

.executive-name {
  margin: 0 0 15px 0;
  color: #ffffff;
  font-size: 24px;
  font-weight: 700;
  display: flex;
  flex-direction: column;
  gap: 4px;
}

.name-first {
  font-weight: 500;
  font-size: 18px;
  color: rgba(255, 255, 255, 0.8);
}

.name-last {
  font-weight: 800;
  text-transform: uppercase;
}

/* Indicateur de statut */
.card-status-indicator {
  display: flex;
  align-items: center;
  gap: 8px;
  justify-content: center;
  margin-top: 15px;
  padding: 8px 16px;
  background: rgba(16, 185, 129, 0.2);
  border-radius: 20px;
  border: 1px solid rgba(16, 185, 129, 0.3);
}

.status-dot {
  width: 8px;
  height: 8px;
  background: var(--success);
  border-radius: 50%;
  animation: pulse 2s ease-in-out infinite;
}

@keyframes pulse {
  0%, 100% { opacity: 1; }
  50% { opacity: 0.5; }
}

.status-text {
  color: rgba(255, 255, 255, 0.7);
  font-size: 12px;
  font-weight: 600;
  text-transform: uppercase;
}

/* Panneau d'actions */
.action-panel-slide {
  position: absolute;
  bottom: 0;
  left: 0;
  right: 0;
  background: var(--dark-bg);
  backdrop-filter: blur(20px);
  border-top: 1px solid rgba(79, 70, 229, 0.5);
  padding: 20px;
  transform: translateY(100%);
  transition: transform 0.4s ease;
  z-index: 10;
}

.executive-card:hover .action-panel-slide {
  transform: translateY(0);
}

.action-panel-header {
  text-align: center;
  margin-bottom: 15px;
}

.action-icon-main {
  font-size: 28px;
  margin-bottom: 8px;
}

.action-panel-header h5 {
  margin: 0;
  color: #ffffff;
  font-size: 16px;
  font-weight: 700;
  text-transform: uppercase;
  letter-spacing: 0.5px;
}

.action-buttons-slide {
  display: flex;
  flex-direction: column;
  gap: 10px;
}

.btn-action-slide {
  width: 100%;
  padding: 12px 16px;
  border: 1px solid;
  border-radius: 10px;
  font-size: 14px;
  font-weight: 700;
  text-transform: uppercase;
  letter-spacing: 0.5px;
  cursor: pointer;
  transition: all 0.3s ease;
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 8px;
}

.btn-edit-slide {
  background: rgba(79, 70, 229, 0.2);
  border-color: var(--primary);
  color: var(--accent);
}

.btn-edit-slide:hover {
  background: rgba(79, 70, 229, 0.4);
  transform: translateY(-2px);
}

.btn-delete-slide {
  background: rgba(239, 68, 68, 0.2);
  border-color: var(--danger);
  color: #ef4444;
}

.btn-delete-slide:hover {
  background: rgba(239, 68, 68, 0.4);
  transform: translateY(-2px);
}

.btn-slide-icon {
  font-size: 16px;
}

.btn-slide-glow {
  display: none;
}

/* Coins décoratifs */
.card-corner-decor {
  position: absolute;
  width: 30px;
  height: 30px;
  border: 2px solid var(--gold);
  opacity: 0.4;
  transition: opacity 0.3s;
}

.executive-card:hover .card-corner-decor {
  opacity: 0.8;
}

.card-corner-decor.top-left {
  top: 10px;
  left: 10px;
  border-right: none;
  border-bottom: none;
  border-top-left-radius: 8px;
}

.card-corner-decor.top-right {
  top: 10px;
  right: 10px;
  border-left: none;
  border-bottom: none;
  border-top-right-radius: 8px;
}

.card-corner-decor.bottom-left {
  bottom: 10px;
  left: 10px;
  border-right: none;
  border-top: none;
  border-bottom-left-radius: 8px;
}

.card-corner-decor.bottom-right {
  bottom: 10px;
  right: 10px;
  border-left: none;
  border-top: none;
  border-bottom-right-radius: 8px;
}

/* ========================================
   🎭 MODAL
   ======================================== */
.modal-premium {
  display: none;
  position: fixed;
  z-index: 2000;
  left: 0;
  top: 0;
  width: 100%;
  height: 100%;
  align-items: center;
  justify-content: center;
  padding: 20px;
  opacity: 0;
  transition: opacity 0.3s;
}

.modal-premium.modal-show {
  opacity: 1;
}

.modal-backdrop-blur {
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background: rgba(0, 0, 0, 0.8);
  backdrop-filter: blur(10px);
  z-index: -1;
}

.modal-content-premium {
  position: relative;
  background: var(--card-bg);
  padding: 40px;
  border: 1px solid rgba(100, 116, 139, 0.3);
  border-radius: 20px;
  width: 100%;
  max-width: 800px;
  backdrop-filter: blur(20px);
  box-shadow: 0 20px 60px rgba(0, 0, 0, 0.7);
  animation: scaleIn 0.4s ease-out;
}

.modal-glow-effect {
  display: none;
}

.modal-close-premium {
  position: absolute;
  right: 20px;
  top: 20px;
  width: 40px;
  height: 40px;
  display: flex;
  align-items: center;
  justify-content: center;
  border-radius: 50%;
  background: rgba(239, 68, 68, 0.2);
  border: 1px solid rgba(239, 68, 68, 0.4);
  color: #ef4444;
  font-size: 20px;
  cursor: pointer;
  transition: all 0.3s ease;
}

.modal-close-premium:hover {
  background: rgba(239, 68, 68, 0.4);
  transform: scale(1.1);
}

.modal-header-premium {
  text-align: center;
  margin-bottom: 30px;
}

.modal-icon-premium {
  font-size: 48px;
  margin-bottom: 15px;
}

.modal-header-premium h2 {
  margin: 0 0 15px 0;
  color: #ffffff;
  font-size: 28px;
  font-weight: 700;
}

.modal-decoration {
  width: 80px;
  height: 3px;
  background: var(--primary);
  margin: 0 auto;
  border-radius: 2px;
}

.modal-actions-premium {
  display: flex;
  gap: 15px;
  justify-content: flex-end;
  margin-top: 30px;
}

.btn-cancel-premium {
  padding: 14px 28px;
  background: rgba(100, 116, 139, 0.2);
  border: 1px solid rgba(100, 116, 139, 0.4);
  color: rgba(255, 255, 255, 0.7);
  border-radius: 10px;
  font-size: 15px;
  font-weight: 600;
  text-transform: uppercase;
  cursor: pointer;
  transition: all 0.3s ease;
  display: flex;
  align-items: center;
  gap: 8px;
}

.btn-cancel-premium:hover {
  background: rgba(100, 116, 139, 0.4);
  color: #ffffff;
  transform: translateY(-2px);
}

.btn-save-premium {
  padding: 14px 32px;
  background: var(--primary);
  border: none;
  border-radius: 10px;
  color: #ffffff;
  font-size: 15px;
  font-weight: 700;
  text-transform: uppercase;
  cursor: pointer;
  transition: all 0.3s ease;
  display: flex;
  align-items: center;
  gap: 8px;
}

.btn-save-premium:hover {
  background: var(--primary-dark);
  transform: translateY(-2px);
  box-shadow: 0 6px 20px rgba(79, 70, 229, 0.4);
}

/* ========================================
   🚫 ÉTAT VIDE
   ======================================== */
.empty-state-premium {
  grid-column: 1 / -1;
  text-align: center;
  padding: 60px 30px;
  background: var(--card-bg);
  border-radius: 20px;
  border: 2px dashed rgba(100, 116, 139, 0.3);
}

.empty-icon {
  font-size: 64px;
  margin-bottom: 15px;
  opacity: 0.5;
}

.empty-state-premium p {
  color: rgba(255, 255, 255, 0.6);
  font-size: 16px;
  margin: 0;
}

.empty-decoration {
  width: 60px;
  height: 2px;
  background: var(--primary);
  margin: 15px auto 0;
  border-radius: 2px;
}

/* ========================================
   📱 RESPONSIVE
   ======================================== */
@media (max-width: 768px) {
  .executive-title {
    font-size: 32px;
    flex-direction: column;
  }
  
  .search-filter-form {
    flex-direction: column;
  }
  
  .page-search-input {
    width: 100%;
    min-width: 0;
  }
  
  .stats-bar {
    gap: 12px;
  }
  
  .stat-item {
    padding: 15px 20px;
    min-width: 100px;
  }
  
  .stat-value {
    font-size: 28px;
  }
  
  .executive-grid {
    grid-template-columns: 1fr;
    gap: 20px;
  }
  
  .executive-card {
    height: 480px;
  }
  
  .form-grid-premium {
    grid-template-columns: 1fr;
  }
  
  .modal-content-premium {
    padding: 25px;
  }
  
  .modal-actions-premium {
    flex-direction: column;
  }
  
  .btn-cancel-premium,
  .btn-save-premium {
    width: 100%;
  }
}
</style>

<%- include('partials/footer') %>

/* ========================================
   ✨ ANIMATIONS KEYFRAMES
   ======================================== */
@keyframes particleFloat {
  0% {
    transform: translateY(100vh) translateX(0) scale(0);
    opacity: 0;
  }
  10% {
    opacity: 1;
  }
  90% {
    opacity: 1;
  }
  100% {
    transform: translateY(-100vh) translateX(100px) scale(1);
    opacity: 0;
  }
}

@keyframes lightRayMove {
  0% {
    transform: translateX(-100%) rotate(45deg);
    opacity: 0;
  }
  50% {
    opacity: 0.3;
  }
  100% {
    transform: translateX(200%) rotate(45deg);
    opacity: 0;
  }
}

@keyframes hologramEffect {
  0%, 100% {
    opacity: 0.1;
    transform: translateY(0);
  }
  50% {
    opacity: 0.3;
    transform: translateY(-100%);
  }
}

@keyframes shimmerMove {
  0% {
    transform: translateX(-100%) translateY(-100%) rotate(45deg);
  }
  100% {
    transform: translateX(200%) translateY(200%) rotate(45deg);
  }
}

@keyframes glowPulse {
  0%, 100% {
    opacity: 0.5;
    transform: scale(1);
  }
  50% {
    opacity: 1;
    transform: scale(1.1);
  }
}

@keyframes rotateGradient {
  0% {
    transform: rotate(0deg);
  }
  100% {
    transform: rotate(360deg);
  }
}

@keyframes slideInUp {
  from {
    opacity: 0;
    transform: translateY(50px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

@keyframes bounceIn {
  0% {
    opacity: 0;
    transform: scale(0.3);
  }
  50% {
    opacity: 1;
    transform: scale(1.05);
  }
  70% {
    transform: scale(0.9);
  }
  100% {
    transform: scale(1);
  }
}

@keyframes neonGlow {
  0%, 100% {
    box-shadow: 0 0 5px var(--neon-blue),
                0 0 10px var(--neon-blue),
                0 0 20px var(--neon-blue),
                0 0 40px var(--neon-blue);
  }
  50% {
    box-shadow: 0 0 10px var(--neon-purple),
                0 0 20px var(--neon-purple),
                0 0 40px var(--neon-purple),
                0 0 80px var(--neon-purple);
  }
}

@keyframes gradientFlow {
  0% {
    background-position: 0% 50%;
  }
  50% {
    background-position: 100% 50%;
  }
  100% {
    background-position: 0% 50%;
  }
}

@keyframes lightSweep {
  0% {
    transform: translateX(-200%);
    opacity: 0;
  }
  50% {
    opacity: 1;
  }
  100% {
    transform: translateX(200%);
    opacity: 0;
  }
}

@keyframes photoParticleFloat {
  0%, 100% {
    transform: translate(0, 0) scale(0);
    opacity: 0;
  }
  50% {
    opacity: 1;
    transform: translate(var(--tx), var(--ty)) scale(1);
  }
}

@keyframes statusPulse {
  0%, 100% {
    opacity: 1;
    transform: scale(1);
  }
  50% {
    opacity: 0.6;
    transform: scale(1.2);
  }
}

/* ========================================
   🌌 EFFETS D'ARRIÈRE-PLAN
   ======================================== */
.particles-container {
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  overflow: hidden;
  pointer-events: none;
  z-index: 0;
}

.particle {
  position: absolute;
  bottom: -50px;
  width: 4px;
  height: 4px;
  background: radial-gradient(circle, var(--neon-blue) 0%, transparent 70%);
  border-radius: 50%;
  animation: particleFloat linear infinite;
  box-shadow: 0 0 10px var(--neon-blue);
}

.particle:nth-child(2n) {
  background: radial-gradient(circle, var(--neon-purple) 0%, transparent 70%);
  box-shadow: 0 0 10px var(--neon-purple);
  width: 6px;
  height: 6px;
}

.particle:nth-child(3n) {
  background: radial-gradient(circle, var(--neon-gold) 0%, transparent 70%);
  box-shadow: 0 0 10px var(--neon-gold);
  width: 3px;
  height: 3px;
}

.light-rays {
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  overflow: hidden;
  pointer-events: none;
  z-index: 0;
  opacity: 0.3;
}

.light-ray {
  position: absolute;
  top: -50%;
  left: -10%;
  width: 100px;
  height: 200%;
  background: linear-gradient(90deg, 
    transparent 0%, 
    rgba(102, 126, 234, 0.3) 50%, 
    transparent 100%);
  animation: lightRayMove 15s linear infinite;
}

.light-ray:nth-child(2) {
  animation-delay: 5s;
  background: linear-gradient(90deg, 
    transparent 0%, 
    rgba(191, 0, 255, 0.3) 50%, 
    transparent 100%);
}

.light-ray:nth-child(3) {
  animation-delay: 10s;
  background: linear-gradient(90deg, 
    transparent 0%, 
    rgba(255, 215, 0, 0.2) 50%, 
    transparent 100%);
}

.executive-suite {
  position: relative;
  z-index: 1;
}

/* ========================================
   🔍 BARRE DE RECHERCHE PREMIUM
   ======================================== */
.page-toolbar {
  margin-bottom: 40px;
  animation: slideInUp 0.8s cubic-bezier(0.34, 1.56, 0.64, 1);
}

.search-filter-form {
  display: flex;
  gap: 15px;
  align-items: center;
  padding: 25px 30px;
  background: linear-gradient(135deg, 
    rgba(102, 126, 234, 0.15) 0%, 
    rgba(118, 75, 162, 0.15) 100%);
  border-radius: 25px;
  border: 2px solid rgba(102, 126, 234, 0.3);
  backdrop-filter: blur(20px);
  box-shadow: 0 15px 45px rgba(0, 0, 0, 0.3),
              inset 0 1px 0 rgba(255, 255, 255, 0.1);
  position: relative;
  overflow: hidden;
}

.search-filter-form::before {
  content: '';
  position: absolute;
  top: -50%;
  left: -50%;
  width: 200%;
  height: 200%;
  background: linear-gradient(45deg,
    transparent 30%,
    rgba(255, 255, 255, 0.05) 50%,
    transparent 70%);
  animation: shimmerMove 3s linear infinite;
  pointer-events: none;
}

.page-search-input {
  flex: 1;
  min-width: 300px;
  padding: 18px 25px;
  border: 2px solid transparent;
  border-radius: 20px;
  background: linear-gradient(135deg, 
    rgba(0, 0, 0, 0.3) 0%, 
    rgba(0, 0, 0, 0.2) 100%);
  color: #ffffff;
  font-size: 16px;
  font-weight: 500;
  transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
  box-shadow: inset 0 2px 10px rgba(0, 0, 0, 0.3);
}

.page-search-input::placeholder {
  color: rgba(255, 255, 255, 0.5);
}

.page-search-input:focus {
  outline: none;
  border-color: var(--neon-blue);
  background: rgba(0, 0, 0, 0.4);
  box-shadow: 0 0 0 4px rgba(0, 243, 255, 0.2),
              0 0 20px rgba(0, 243, 255, 0.3),
              inset 0 2px 10px rgba(0, 0, 0, 0.3);
  transform: translateY(-2px);
}

.page-search-btn {
  padding: 18px 35px;
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  background-size: 200% 200%;
  border: none;
  border-radius: 20px;
  color: #ffffff;
  font-size: 16px;
  font-weight: 700;
  cursor: pointer;
  transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
  box-shadow: 0 10px 30px rgba(102, 126, 234, 0.4);
  position: relative;
  overflow: hidden;
  display: flex;
  align-items: center;
  gap: 10px;
}

.page-search-btn::before {
  content: '';
  position: absolute;
  top: 50%;
  left: 50%;
  width: 0;
  height: 0;
  background: radial-gradient(circle, rgba(255, 255, 255, 0.3) 0%, transparent 70%);
  border-radius: 50%;
  transform: translate(-50%, -50%);
  transition: width 0.6s, height 0.6s;
}

.page-search-btn:hover::before {
  width: 400px;
  height: 400px;
}

.page-search-btn:hover {
  transform: translateY(-3px) scale(1.05);
  box-shadow: 0 15px 40px rgba(102, 126, 234, 0.6);
  animation: gradientFlow 3s ease infinite;
}

.search-icon {
  font-size: 20px;
  animation: glowPulse 2s ease-in-out infinite;
}

.btn-reset {
  padding: 18px 28px;
  background: linear-gradient(135deg, 
    rgba(245, 87, 108, 0.3) 0%, 
    rgba(240, 147, 251, 0.3) 100%);
  border: 2px solid rgba(245, 87, 108, 0.5);
  border-radius: 20px;
  color: #ff6b95;
  text-decoration: none;
  font-size: 15px;
  font-weight: 700;
  transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
  box-shadow: 0 10px 30px rgba(245, 87, 108, 0.3);
}

.btn-reset:hover {
  background: linear-gradient(135deg, 
    rgba(245, 87, 108, 0.5) 0%, 
    rgba(240, 147, 251, 0.5) 100%);
  transform: translateY(-3px);
  box-shadow: 0 15px 40px rgba(245, 87, 108, 0.5);
}

/* ========================================
   👑 EN-TÊTE EXÉCUTIF
   ======================================== */
.page-header {
  margin-bottom: 50px;
  text-align: center;
  padding: 60px 40px;
  background: linear-gradient(135deg, 
    rgba(102, 126, 234, 0.2) 0%, 
    rgba(118, 75, 162, 0.2) 100%);
  border-radius: 30px;
  border: 2px solid rgba(102, 126, 234, 0.4);
  position: relative;
  overflow: hidden;
  box-shadow: 0 20px 60px rgba(0, 0, 0, 0.4),
              inset 0 1px 0 rgba(255, 255, 255, 0.1);
  backdrop-filter: blur(20px);
  animation: slideInUp 1s cubic-bezier(0.34, 1.56, 0.64, 1) 0.2s both;
}

.header-glow {
  position: absolute;
  top: 50%;
  left: 50%;
  width: 300px;
  height: 300px;
  background: radial-gradient(circle, rgba(102, 126, 234, 0.4) 0%, transparent 70%);
  transform: translate(-50%, -50%);
  animation: glowPulse 4s ease-in-out infinite;
  pointer-events: none;
}

.executive-title {
  margin: 0 0 20px 0;
  font-size: 56px;
  font-weight: 900;
  letter-spacing: 4px;
  text-transform: uppercase;
  background: linear-gradient(135deg, 
    #ffd700 0%, 
    #ffed4e 25%, 
    #ff8c00 50%, 
    #ffd700 75%, 
    #ffed4e 100%);
  background-size: 300% 300%;
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
  animation: gradientFlow 5s ease infinite;
  text-shadow: 0 0 40px rgba(255, 215, 0, 0.5);
  position: relative;
  z-index: 1;
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 20px;
}

.title-icon {
  font-size: 48px;
  animation: bounceIn 1s ease 0.5s both, glowPulse 3s ease-in-out infinite 1.5s;
}

.executive-subtitle {
  color: rgba(255, 255, 255, 0.8);
  font-size: 20px;
  font-weight: 600;
  letter-spacing: 3px;
  text-transform: uppercase;
  margin: 0 0 40px 0;
  position: relative;
  z-index: 1;
}

/* ========================================
   📊 STATISTIQUES PREMIUM
   ======================================== */
.stats-bar {
  display: flex;
  justify-content: center;
  gap: 25px;
  flex-wrap: wrap;
  position: relative;
  z-index: 1;
}

.stat-item {
  position: relative;
  background: linear-gradient(135deg, 
    rgba(0, 0, 0, 0.4) 0%, 
    rgba(0, 0, 0, 0.2) 100%);
  border: 2px solid;
  border-radius: 20px;
  padding: 25px 40px;
  min-width: 140px;
  text-align: center;
  backdrop-filter: blur(15px);
  transition: all 0.5s cubic-bezier(0.4, 0, 0.2, 1);
  box-shadow: 0 10px 30px rgba(0, 0, 0, 0.3),
              inset 0 1px 0 rgba(255, 255, 255, 0.1);
  overflow: hidden;
}

.stat-primary {
  border-color: var(--neon-gold);
}

.stat-gradient {
  border-color: var(--neon-blue);
}

.stat-item:hover {
  transform: translateY(-10px) scale(1.08);
  box-shadow: 0 20px 50px rgba(0, 0, 0, 0.5);
}

.stat-primary:hover {
  box-shadow: 0 0 40px rgba(255, 215, 0, 0.6),
              0 20px 50px rgba(0, 0, 0, 0.5);
}

.stat-gradient:hover {
  box-shadow: 0 0 40px rgba(0, 243, 255, 0.6),
              0 20px 50px rgba(0, 0, 0, 0.5);
}

.stat-icon {
  font-size: 32px;
  margin-bottom: 10px;
  animation: bounceIn 1s ease both;
}

.stat-value {
  font-size: 48px;
  font-weight: 900;
  color: #ffffff;
  margin-bottom: 8px;
  text-shadow: 0 0 20px rgba(255, 255, 255, 0.5);
  transition: all 0.3s;
}

.stat-primary .stat-value {
  background: var(--gold-gradient);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
}

.stat-gradient .stat-value {
  color: var(--neon-blue);
}

.stat-item:hover .stat-value {
  transform: scale(1.2);
}

.stat-label {
  font-size: 13px;
  color: rgba(255, 255, 255, 0.7);
  text-transform: uppercase;
  letter-spacing: 2px;
  font-weight: 700;
}

.stat-shine {
  position: absolute;
  top: -50%;
  left: -50%;
  width: 200%;
  height: 200%;
  background: linear-gradient(45deg,
    transparent 30%,
    rgba(255, 255, 255, 0.1) 50%,
    transparent 70%);
  animation: shimmerMove 3s linear infinite;
  pointer-events: none;
}

/* ========================================
   📝 FORMULAIRE PREMIUM
   ======================================== */
.executive-form {
  background: linear-gradient(135deg, 
    rgba(102, 126, 234, 0.15) 0%, 
    rgba(118, 75, 162, 0.15) 100%);
  border: 2px solid rgba(102, 126, 234, 0.4);
  margin-bottom: 50px;
  border-radius: 30px;
  padding: 40px;
  box-shadow: 0 20px 60px rgba(0, 0, 0, 0.4),
              inset 0 1px 0 rgba(255, 255, 255, 0.1);
  backdrop-filter: blur(20px);
  position: relative;
  overflow: hidden;
  animation: slideInUp 1.2s cubic-bezier(0.34, 1.56, 0.64, 1) 0.4s both;
}

.form-header {
  text-align: center;
  margin-bottom: 35px;
}

.form-icon-premium {
  font-size: 48px;
  margin-bottom: 15px;
  animation: bounceIn 1s ease 0.6s both;
}

.form-header h3 {
  color: #ffffff;
  font-size: 28px;
  font-weight: 700;
  letter-spacing: 2px;
  text-transform: uppercase;
  margin: 0 0 15px 0;
  text-shadow: 0 0 20px rgba(102, 126, 234, 0.5);
}

.form-decoration-line {
  width: 100px;
  height: 3px;
  background: var(--gold-gradient);
  margin: 0 auto;
  border-radius: 2px;
  box-shadow: 0 0 15px rgba(255, 215, 0, 0.6);
}

.form-grid-premium {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
  gap: 30px;
  margin-bottom: 35px;
}

.form-group-premium {
  position: relative;
}

.form-group-premium label {
  display: flex;
  align-items: center;
  gap: 10px;
  margin-bottom: 12px;
  color: rgba(255, 255, 255, 0.9);
  font-size: 14px;
  font-weight: 700;
  letter-spacing: 1px;
  text-transform: uppercase;
}

.label-icon {
  font-size: 18px;
}

.form-group-premium input,
.form-group-premium select {
  width: 100%;
  padding: 16px 20px;
  border: 2px solid transparent;
  border-radius: 15px;
  background: rgba(0, 0, 0, 0.3);
  color: #ffffff;
  font-size: 16px;
  font-weight: 500;
  transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
  box-shadow: inset 0 2px 10px rgba(0, 0, 0, 0.3);
}

.form-group-premium input:focus,
.form-group-premium select:focus {
  outline: none;
  border-color: var(--neon-blue);
  background: rgba(0, 0, 0, 0.4);
  box-shadow: 0 0 0 4px rgba(0, 243, 255, 0.2),
              0 0 20px rgba(0, 243, 255, 0.3),
              inset 0 2px 10px rgba(0, 0, 0, 0.3);
  transform: translateY(-2px);
}

.input-underline {
  position: absolute;
  bottom: 0;
  left: 0;
  width: 0;
  height: 2px;
  background: var(--gold-gradient);
  transition: width 0.4s cubic-bezier(0.4, 0, 0.2, 1);
  box-shadow: 0 0 10px rgba(255, 215, 0, 0.6);
}

.form-group-premium input:focus ~ .input-underline,
.form-group-premium select:focus ~ .input-underline {
  width: 100%;
}

.btn-add-premium {
  width: 100%;
  padding: 20px 40px;
  background: var(--primary-gradient);
  background-size: 200% 200%;
  border: none;
  border-radius: 20px;
  color: #ffffff;
  font-size: 18px;
  font-weight: 700;
  text-transform: uppercase;
  letter-spacing: 2px;
  cursor: pointer;
  transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
  box-shadow: 0 15px 40px rgba(102, 126, 234, 0.5);
  position: relative;
  overflow: hidden;
}

.btn-glow {
  position: absolute;
  top: -50%;
  left: -50%;
  width: 200%;
  height: 200%;
  background: radial-gradient(circle, rgba(255, 255, 255, 0.3) 0%, transparent 70%);
  opacity: 0;
  transition: opacity 0.6s;
  pointer-events: none;
}

.btn-add-premium:hover .btn-glow {
  opacity: 1;
  animation: rotateGradient 3s linear infinite;
}

.btn-add-premium:hover {
  transform: translateY(-5px) scale(1.02);
  box-shadow: 0 20px 50px rgba(102, 126, 234, 0.7);
  animation: gradientFlow 3s ease infinite;
}

.btn-content {
  position: relative;
  z-index: 1;
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 12px;
}

.btn-icon {
  font-size: 22px;
}

/* ========================================
   🎴 CARTES EXECUTIVES 3D
   ======================================== */
.list-section {
  animation: slideInUp 1.4s cubic-bezier(0.34, 1.56, 0.64, 1) 0.6s both;
}

.list-header-premium {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 20px;
  margin-bottom: 50px;
  padding: 25px;
  background: linear-gradient(135deg, 
    rgba(102, 126, 234, 0.15) 0%, 
    rgba(118, 75, 162, 0.15) 100%);
  border-radius: 25px;
  border: 2px solid rgba(102, 126, 234, 0.3);
  backdrop-filter: blur(15px);
  box-shadow: 0 10px 30px rgba(0, 0, 0, 0.3);
}

.header-decoration-left,
.header-decoration-right {
  flex: 1;
  height: 2px;
  background: linear-gradient(90deg, 
    transparent 0%, 
    var(--neon-blue) 50%, 
    transparent 100%);
  box-shadow: 0 0 10px var(--neon-blue);
}

.list-header-premium h3 {
  margin: 0;
  color: #ffffff;
  font-size: 32px;
  font-weight: 800;
  letter-spacing: 2px;
  text-transform: uppercase;
  display: flex;
  align-items: center;
  gap: 15px;
  text-shadow: 0 0 20px rgba(102, 126, 234, 0.6);
}

.list-icon {
  font-size: 36px;
  animation: glowPulse 2s ease-in-out infinite;
}

.badge-premium {
  background: var(--gold-gradient);
  color: #000;
  padding: 8px 20px;
  border-radius: 25px;
  font-size: 20px;
  font-weight: 900;
  box-shadow: 0 0 20px rgba(255, 215, 0, 0.6),
              0 5px 15px rgba(0, 0, 0, 0.3);
}

.executive-grid {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(350px, 1fr));
  gap: 40px;
  align-items: start;
  perspective: 1000px;
}

.executive-card-wrapper {
  animation: slideInUp 0.8s cubic-bezier(0.34, 1.56, 0.64, 1) both;
}

/* Carte 3D avec effet zoom et élévation */
.executive-card {
  position: relative;
  height: 550px;
  border-radius: 30px;
  overflow: hidden;
  background: linear-gradient(135deg, 
    rgba(102, 126, 234, 0.2) 0%, 
    rgba(118, 75, 162, 0.2) 100%);
  border: 2px solid rgba(102, 126, 234, 0.4);
  box-shadow: 0 20px 60px rgba(0, 0, 0, 0.5),
              inset 0 1px 0 rgba(255, 255, 255, 0.1);
  backdrop-filter: blur(20px);
  cursor: pointer;
  transition: all 0.6s cubic-bezier(0.4, 0, 0.2, 1);
  transform-style: preserve-3d;
  perspective: 1000px;
  display: flex;
  flex-direction: column;
}

.executive-card:hover {
  transform: translateY(-25px) scale(1.05) rotateX(2deg);
  box-shadow: 0 40px 100px rgba(0, 0, 0, 0.7),
              0 0 80px rgba(102, 126, 234, 0.6),
              inset 0 1px 0 rgba(255, 255, 255, 0.2);
  border-color: rgba(102, 126, 234, 0.8);
}

/* Effet holographique */
.card-hologram {
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 100%;
  background: repeating-linear-gradient(
    0deg,
    rgba(255, 255, 255, 0.03) 0px,
    rgba(255, 255, 255, 0.03) 2px,
    transparent 2px,
    transparent 4px
  );
  pointer-events: none;
  animation: hologramEffect 4s linear infinite;
  z-index: 10;
}

/* Effet de balayage de lumière */
.card-light-sweep {
  position: absolute;
  top: -50%;
  left: -50%;
  width: 200%;
  height: 200%;
  background: linear-gradient(90deg,
    transparent 0%,
    rgba(255, 255, 255, 0) 40%,
    rgba(255, 255, 255, 0.8) 50%,
    rgba(255, 255, 255, 0) 60%,
    transparent 100%);
  transform: translateX(-200%);
  pointer-events: none;
  z-index: 15;
  opacity: 0;
  transition: opacity 0.3s;
}

.executive-card:hover .card-light-sweep {
  animation: lightSweep 1.5s ease-in-out;
  opacity: 1;
}

.card-border-glow {
  position: absolute;
  top: -2px;
  left: -2px;
  right: -2px;
  bottom: -2px;
  background: linear-gradient(45deg, 
    var(--neon-blue), 
    var(--neon-purple), 
    var(--neon-gold), 
    var(--neon-blue));
  background-size: 400% 400%;
  border-radius: 30px;
  z-index: -1;
  opacity: 0;
  transition: opacity 0.4s;
  animation: gradientFlow 4s ease infinite;
  filter: blur(10px);
}

.executive-card:hover .card-border-glow {
  opacity: 0.6;
}

/* Coins décoratifs */
.card-corner-decor {
  position: absolute;
  width: 40px;
  height: 40px;
  border: 2px solid var(--neon-gold);
  opacity: 0.5;
  transition: opacity 0.4s;
  z-index: 5;
}

.executive-card:hover .card-corner-decor {
  opacity: 1;
  box-shadow: 0 0 15px var(--neon-gold);
}

.card-corner-decor.top-left {
  top: 10px;
  left: 10px;
  border-right: none;
  border-bottom: none;
  border-top-left-radius: 10px;
}

.card-corner-decor.top-right {
  top: 10px;
  right: 10px;
  border-left: none;
  border-bottom: none;
  border-top-right-radius: 10px;
}

.card-corner-decor.bottom-left {
  bottom: 10px;
  left: 10px;
  border-right: none;
  border-top: none;
  border-bottom-left-radius: 10px;
}

.card-corner-decor.bottom-right {
  bottom: 10px;
  right: 10px;
  border-left: none;
  border-top: none;
  border-bottom-right-radius: 10px;
}

/* Photo executive */
.executive-photo-container {
  position: relative;
  width: 100%;
  height: 320px;
  overflow: hidden;
  background: linear-gradient(135deg, 
    rgba(0, 0, 0, 0.4) 0%, 
    rgba(0, 0, 0, 0.2) 100%);
}

.photo-frame {
  position: absolute;
  top: 15px;
  left: 15px;
  right: 15px;
  bottom: 15px;
  border: 3px solid rgba(255, 215, 0, 0.3);
  border-radius: 15px;
  z-index: 2;
  pointer-events: none;
  transition: border-color 0.4s;
}

.executive-card:hover .photo-frame {
  border-color: rgba(255, 215, 0, 0.8);
  box-shadow: 0 0 20px rgba(255, 215, 0, 0.6);
}

.photo-glow {
  position: absolute;
  top: 50%;
  left: 50%;
  width: 200px;
  height: 200px;
  background: radial-gradient(circle, rgba(102, 126, 234, 0.4) 0%, transparent 70%);
  transform: translate(-50%, -50%);
  z-index: 1;
  pointer-events: none;
  animation: glowPulse 3s ease-in-out infinite;
}

.executive-photo {
  width: 100%;
  height: 100%;
  object-fit: cover;
  transition: transform 0.6s cubic-bezier(0.4, 0, 0.2, 1);
}

.executive-card:hover .executive-photo {
  transform: scale(1.15);
}

.executive-placeholder {
  width: 100%;
  height: 100%;
  display: flex;
  align-items: center;
  justify-content: center;
  background: linear-gradient(135deg, 
    rgba(102, 126, 234, 0.3) 0%, 
    rgba(118, 75, 162, 0.3) 100%);
}

.placeholder-icon {
  font-size: 120px;
  opacity: 0.5;
}

.photo-overlay {
  position: absolute;
  bottom: 0;
  left: 0;
  right: 0;
  height: 50%;
  background: linear-gradient(to top, 
    rgba(0, 0, 0, 0.8) 0%, 
    transparent 100%);
  z-index: 1;
  pointer-events: none;
}

/* Particules autour de la photo */
.photo-particles {
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  pointer-events: none;
  z-index: 3;
  opacity: 0;
  transition: opacity 0.4s;
}

.executive-card:hover .photo-particles {
  opacity: 1;
}

.photo-particle {
  position: absolute;
  width: 8px;
  height: 8px;
  background: radial-gradient(circle, var(--neon-gold) 0%, transparent 70%);
  border-radius: 50%;
  box-shadow: 0 0 15px var(--neon-gold);
  animation: photoParticleFloat 2s ease-in-out infinite;
}

.photo-particle:nth-child(1) {
  top: 10%;
  left: 10%;
  --tx: 20px;
  --ty: -30px;
  animation-delay: 0s;
}

.photo-particle:nth-child(2) {
  top: 20%;
  right: 15%;
  --tx: -25px;
  --ty: -20px;
  animation-delay: 0.3s;
  background: radial-gradient(circle, var(--neon-blue) 0%, transparent 70%);
  box-shadow: 0 0 15px var(--neon-blue);
}

.photo-particle:nth-child(3) {
  bottom: 20%;
  left: 20%;
  --tx: -20px;
  --ty: 25px;
  animation-delay: 0.6s;
  background: radial-gradient(circle, var(--neon-purple) 0%, transparent 70%);
  box-shadow: 0 0 15px var(--neon-purple);
}

.photo-particle:nth-child(4) {
  bottom: 15%;
  right: 10%;
  --tx: 30px;
  --ty: 20px;
  animation-delay: 0.9s;
}

/* Contenu de la carte */
.card-content-premium {
  flex: 1;
  padding: 30px;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  text-align: center;
  position: relative;
  z-index: 2;
}

.executive-rank-badge {
  position: relative;
  display: inline-flex;
  align-items: center;
  gap: 8px;
  padding: 10px 25px;
  background: var(--gold-gradient);
  border-radius: 25px;
  margin-bottom: 20px;
  box-shadow: 0 0 25px rgba(255, 215, 0, 0.6),
              0 5px 15px rgba(0, 0, 0, 0.3);
  overflow: hidden;
}

.rank-icon {
  font-size: 20px;
  animation: glowPulse 2s ease-in-out infinite;
}

.rank-text {
  color: #000;
  font-size: 14px;
  font-weight: 900;
  text-transform: uppercase;
  letter-spacing: 1px;
}

.badge-shine {
  position: absolute;
  top: 0;
  left: -100%;
  width: 100%;
  height: 100%;
  background: linear-gradient(90deg, 
    transparent 0%, 
    rgba(255, 255, 255, 0.6) 50%, 
    transparent 100%);
  animation: shimmerMove 3s linear infinite;
}

.executive-name {
  margin: 0 0 20px 0;
  color: #ffffff;
  font-size: 28px;
  font-weight: 800;
  letter-spacing: 1px;
  text-shadow: 0 0 20px rgba(255, 255, 255, 0.3);
  display: flex;
  flex-direction: column;
  gap: 5px;
}

.name-first {
  font-weight: 600;
  font-size: 20px;
  color: rgba(255, 255, 255, 0.8);
}

.name-last {
  font-weight: 900;
  text-transform: uppercase;
}

/* Indicateur de statut */
.card-status-indicator {
  display: flex;
  align-items: center;
  gap: 10px;
  justify-content: center;
  margin-top: 20px;
  padding: 10px 20px;
  background: rgba(0, 0, 0, 0.3);
  border-radius: 25px;
  border: 1px solid rgba(102, 126, 234, 0.3);
}

.status-dot {
  width: 10px;
  height: 10px;
  background: var(--neon-blue);
  border-radius: 50%;
  box-shadow: 0 0 15px var(--neon-blue);
  animation: statusPulse 2s ease-in-out infinite;
}

.status-text {
  color: rgba(255, 255, 255, 0.7);
  font-size: 13px;
  font-weight: 600;
  letter-spacing: 0.5px;
  text-transform: uppercase;
}

/* Panneau d'actions qui glisse depuis le bas */
.action-panel-slide {
  position: absolute;
  bottom: 0;
  left: 0;
  right: 0;
  background: linear-gradient(135deg,
    rgba(0, 0, 0, 0.95) 0%,
    rgba(26, 26, 46, 0.95) 100%);
  backdrop-filter: blur(20px);
  border-top: 2px solid rgba(102, 126, 234, 0.5);
  padding: 25px;
  transform: translateY(100%);
  transition: transform 0.5s cubic-bezier(0.4, 0, 0.2, 1);
  z-index: 20;
  box-shadow: 0 -10px 40px rgba(0, 0, 0, 0.8);
}

.executive-card:hover .action-panel-slide {
  transform: translateY(0);
}

.action-panel-header {
  text-align: center;
  margin-bottom: 20px;
}

.action-icon-main {
  font-size: 36px;
  margin-bottom: 10px;
  animation: glowPulse 2s ease-in-out infinite;
}

.action-panel-header h5 {
  margin: 0;
  color: #ffffff;
  font-size: 18px;
  font-weight: 700;
  letter-spacing: 1px;
  text-transform: uppercase;
  text-shadow: 0 0 15px rgba(102, 126, 234, 0.5);
}

.action-buttons-slide {
  display: flex;
  flex-direction: column;
  gap: 12px;
}

.btn-action-slide {
  width: 100%;
  padding: 15px 20px;
  border: 2px solid;
  border-radius: 15px;
  font-size: 15px;
  font-weight: 700;
  text-transform: uppercase;
  letter-spacing: 1px;
  cursor: pointer;
  transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 10px;
  position: relative;
  overflow: hidden;
  backdrop-filter: blur(10px);
}

.btn-edit-slide {
  background: linear-gradient(135deg,
    rgba(0, 243, 255, 0.2) 0%,
    rgba(102, 126, 234, 0.2) 100%);
  border-color: var(--neon-blue);
  color: var(--neon-blue);
  box-shadow: 0 0 20px rgba(0, 243, 255, 0.3);
}

.btn-edit-slide:hover {
  background: linear-gradient(135deg,
    rgba(0, 243, 255, 0.4) 0%,
    rgba(102, 126, 234, 0.4) 100%);
  box-shadow: 0 0 30px rgba(0, 243, 255, 0.6);
  transform: translateY(-3px) scale(1.02);
}

.btn-delete-slide {
  background: linear-gradient(135deg,
    rgba(245, 87, 108, 0.2) 0%,
    rgba(240, 147, 251, 0.2) 100%);
  border-color: #f5576c;
  color: #ff6b95;
  box-shadow: 0 0 20px rgba(245, 87, 108, 0.3);
}

.btn-delete-slide:hover {
  background: linear-gradient(135deg,
    rgba(245, 87, 108, 0.4) 0%,
    rgba(240, 147, 251, 0.4) 100%);
  box-shadow: 0 0 30px rgba(245, 87, 108, 0.6);
  transform: translateY(-3px) scale(1.02);
}

.btn-slide-icon {
  font-size: 18px;
}

.btn-slide-glow {
  position: absolute;
  top: 50%;
  left: 50%;
  width: 0;
  height: 0;
  border-radius: 50%;
  background: rgba(255, 255, 255, 0.4);
  transform: translate(-50%, -50%);
  transition: width 0.6s, height 0.6s;
}

.btn-action-slide:hover .btn-slide-glow {
  width: 400px;
  height: 400px;
}

/* ========================================
   🎭 MODAL PREMIUM
   ======================================== */
.modal-premium {
  display: none;
  position: fixed;
  z-index: 2000;
  left: 0;
  top: 0;
  width: 100%;
  height: 100%;
  align-items: center;
  justify-content: center;
  overflow-y: auto;
  padding: 20px;
  opacity: 0;
  transition: opacity 0.3s;
}

.modal-premium.modal-show {
  opacity: 1;
}

.modal-backdrop-blur {
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background: rgba(0, 0, 0, 0.9);
  backdrop-filter: blur(15px);
  z-index: -1;
}

.modal-content-premium {
  position: relative;
  background: linear-gradient(135deg, 
    rgba(26, 26, 46, 0.98) 0%, 
    rgba(26, 35, 50, 0.98) 100%);
  padding: 50px;
  border: 2px solid rgba(102, 126, 234, 0.5);
  border-radius: 30px;
  width: 100%;
  max-width: 900px;
  box-shadow: 0 30px 90px rgba(0, 0, 0, 0.8),
              0 0 60px rgba(102, 126, 234, 0.3),
              inset 0 1px 0 rgba(255, 255, 255, 0.1);
  backdrop-filter: blur(30px);
  animation: bounceIn 0.6s cubic-bezier(0.34, 1.56, 0.64, 1);
  overflow: hidden;
}

.modal-glow-effect {
  position: absolute;
  top: 50%;
  left: 50%;
  width: 400px;
  height: 400px;
  background: radial-gradient(circle, rgba(102, 126, 234, 0.3) 0%, transparent 70%);
  transform: translate(-50%, -50%);
  animation: glowPulse 4s ease-in-out infinite;
  pointer-events: none;
  z-index: 0;
}

.modal-close-premium {
  position: absolute;
  right: 25px;
  top: 25px;
  width: 50px;
  height: 50px;
  display: flex;
  align-items: center;
  justify-content: center;
  border-radius: 50%;
  background: linear-gradient(135deg, 
    rgba(245, 87, 108, 0.2) 0%, 
    rgba(240, 147, 251, 0.2) 100%);
  border: 2px solid rgba(245, 87, 108, 0.4);
  color: #ff6b95;
  font-size: 24px;
  font-weight: bold;
  cursor: pointer;
  transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
  z-index: 10;
  box-shadow: 0 0 20px rgba(245, 87, 108, 0.3);
}

.modal-close-premium:hover {
  background: linear-gradient(135deg, 
    rgba(245, 87, 108, 0.5) 0%, 
    rgba(240, 147, 251, 0.5) 100%);
  transform: rotate(90deg) scale(1.1);
  box-shadow: 0 0 30px rgba(245, 87, 108, 0.6);
}

.modal-header-premium {
  text-align: center;
  margin-bottom: 40px;
  position: relative;
  z-index: 1;
}

.modal-icon-premium {
  font-size: 56px;
  margin-bottom: 20px;
  animation: bounceIn 1s ease 0.3s both;
}

.modal-header-premium h2 {
  margin: 0 0 20px 0;
  color: #ffffff;
  font-size: 36px;
  font-weight: 800;
  letter-spacing: 2px;
  text-transform: uppercase;
  text-shadow: 0 0 25px rgba(102, 126, 234, 0.5);
}

.modal-decoration {
  width: 120px;
  height: 3px;
  background: var(--gold-gradient);
  margin: 0 auto;
  border-radius: 2px;
  box-shadow: 0 0 15px rgba(255, 215, 0, 0.6);
}

.modal-actions-premium {
  display: flex;
  gap: 20px;
  justify-content: flex-end;
  margin-top: 40px;
  position: relative;
  z-index: 1;
}

.btn-cancel-premium {
  padding: 16px 32px;
  background: linear-gradient(135deg, 
    rgba(128, 128, 128, 0.2) 0%, 
    rgba(100, 100, 100, 0.2) 100%);
  border: 2px solid rgba(128, 128, 128, 0.4);
  color: rgba(255, 255, 255, 0.7);
  border-radius: 15px;
  font-size: 16px;
  font-weight: 700;
  text-transform: uppercase;
  letter-spacing: 1px;
  cursor: pointer;
  transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
  display: flex;
  align-items: center;
  gap: 10px;
}

.btn-cancel-premium:hover {
  background: linear-gradient(135deg, 
    rgba(128, 128, 128, 0.4) 0%, 
    rgba(100, 100, 100, 0.4) 100%);
  color: #ffffff;
  transform: translateY(-3px);
  box-shadow: 0 10px 30px rgba(128, 128, 128, 0.4);
}

.btn-save-premium {
  padding: 16px 40px;
  background: var(--primary-gradient);
  background-size: 200% 200%;
  border: none;
  border-radius: 15px;
  color: #ffffff;
  font-size: 16px;
  font-weight: 700;
  text-transform: uppercase;
  letter-spacing: 1px;
  cursor: pointer;
  transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
  box-shadow: 0 10px 30px rgba(102, 126, 234, 0.5);
  position: relative;
  overflow: hidden;
}

.btn-save-premium:hover {
  transform: translateY(-3px) scale(1.05);
  box-shadow: 0 15px 40px rgba(102, 126, 234, 0.7);
  animation: gradientFlow 3s ease infinite;
}

.btn-save-premium .btn-content {
  display: flex;
  align-items: center;
  gap: 10px;
}

/* ========================================
   🚫 ÉTAT VIDE
   ======================================== */
.empty-state-premium {
  grid-column: 1 / -1;
  text-align: center;
  padding: 80px 40px;
  background: linear-gradient(135deg, 
    rgba(102, 126, 234, 0.1) 0%, 
    rgba(118, 75, 162, 0.1) 100%);
  border-radius: 25px;
  border: 2px dashed rgba(102, 126, 234, 0.3);
  backdrop-filter: blur(10px);
}

.empty-icon {
  font-size: 80px;
  margin-bottom: 20px;
  opacity: 0.5;
  animation: glowPulse 3s ease-in-out infinite;
}

.empty-state-premium p {
  color: rgba(255, 255, 255, 0.6);
  font-size: 20px;
  font-weight: 600;
  letter-spacing: 1px;
  margin: 0;
}

.empty-decoration {
  width: 100px;
  height: 3px;
  background: var(--primary-gradient);
  margin: 20px auto 0;
  border-radius: 2px;
}

/* ========================================
   📱 RESPONSIVE
   ======================================== */
@media (max-width: 1024px) {
  .executive-title {
    font-size: 42px;
  }
  
  .executive-grid {
    grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
    gap: 30px;
  }
  
  .executive-card {
    height: 500px;
  }
}

@media (max-width: 768px) {
  .executive-title {
    font-size: 32px;
    flex-direction: column;
  }
  
  .title-icon {
    font-size: 36px;
  }
  
  .executive-subtitle {
    font-size: 16px;
  }
  
  .search-filter-form {
    flex-direction: column;
    padding: 20px;
  }
  
  .page-search-input {
    width: 100%;
    min-width: 0;
  }
  
  .stats-bar {
    gap: 15px;
  }
  
  .stat-item {
    padding: 20px 30px;
    min-width: 110px;
  }
  
  .stat-value {
    font-size: 36px;
  }
  
  .executive-grid {
    grid-template-columns: 1fr;
    gap: 25px;
  }
  
  .executive-card {
    height: 520px;
  }
  
  .form-grid-premium {
    grid-template-columns: 1fr;
  }
  
  .modal-content-premium {
    padding: 30px 20px;
  }
  
  .modal-header-premium h2 {
    font-size: 28px;
  }
  
  .modal-actions-premium {
    flex-direction: column;
  }
  
  .btn-cancel-premium,
  .btn-save-premium {
    width: 100%;
  }
  
  .page-header {
    padding: 40px 20px;
  }
  
  .executive-form {
    padding: 25px 20px;
  }
}

@media (max-width: 480px) {
  .executive-title {
    font-size: 24px;
  }
  
  .list-header-premium h3 {
    font-size: 24px;
    flex-direction: column;
  }
  
  .executive-photo-container {
    height: 280px;
  }
  
  .executive-name {
    font-size: 24px;
  }
}
</style>

<%- include('partials/footer') %>


</body></html>
