<nav class="topbar">
  <div class="brand">Casino</div>

  <div class="links">
    <a class="<%= currentPath === '/' ? 'active':'' %>" href="/">Accueil</a>
    <a class="<%= currentPath.startsWith('/blacklist') ? 'active':'' %>" href="/blacklist">BlackList</a>
    <a class="<%= currentPath.startsWith('/employes') ? 'active':'' %>" href="/employes">Partenariat Employes</a>
    <a class="<%= currentPath.startsWith('/employeurs') ? 'active':'' %>" href="/employeurs">Employeurs</a>
    <a class="<%= currentPath.startsWith('/entreprises') ? 'active':'' %>" href="/entreprises">Partenariat Entreprise</a>
    <a class="<%= currentPath.startsWith('/menu') ? 'active':'' %>" href="/menu">Menu</a>
    <a class="<%= currentPath.startsWith('/reglement') ? 'active':'' %>" href="/reglement">Règlement</a>

    <% if (user) { %>
      <a class="<%= currentPath.startsWith('/dashboard') ? 'active':'' %>" href="/dashboard">Dashboard</a>
      <a class="<%= currentPath.startsWith('/avertissements') ? 'active':'' %>" href="/avertissements">Avertissements</a>

      <form method="post" action="/logout" class="inline">
        <button class="btn small" type="submit">Déconnexion</button>
      </form>
    <% } else { %>
      <a class="<%= currentPath.startsWith('/login') ? 'active':'' %>" href="/login">Connexion</a>
    <% } %>
  </div>
</nav>
